---
phase: quick
plan: 260917-l7l
type: execute
wave: 1
depends_on: []
files_modified:
  - web/tests/comptes.test.tsx
  - web/src/pages/comptes/LigneDroit.tsx
  - web/src/pages/comptes/dialogues.tsx
  - web/src/pages/comptes/DetailCompte.tsx
autonomous: true
requirements: [PERM-02, PERM-03]

must_haves:
  truths:
    - "`Par magasin` est rendu visible en permanence sur chaque ligne de droit d'un compte a 2 magasins ou plus, sans survol, sans focus, et donc aussi sur une tablette de comptoir"
    - "Le defaut de CLAUDE.md #13 ne bouge pas : la case a cocher reste UNIFORME par defaut, aucune ligne n'est depliee d'emblee sauf si elle est deja personnalisee, et la forme de stockage (gerant, magasin, permission) n'est pas touchee"
    - "L'existence meme du bouton reste decidee par `magasinsAccordes.length >= 2` : seule la revelation change"
    - "`Reinitialiser le mot de passe` ouvre une confirmation qui nomme la consequence avant d'agir ; aucun appel reseau ne part avant la confirmation"
    - "Le bouton secondaire de cette confirmation est `Retour`, jamais `Annuler` (7.10 / 9.3)"
    - "Apres confirmation, le comportement est exactement celui d'aujourd'hui : la mutation part et `DialogueMotDePasse` montre le mot de passe provisoire"
    - "Aucun changement serveur : ni vue, ni serialiseur, ni route, donc `web/src/api/schema.yml` n'est pas regenere"
  artifacts:
    - path: "web/tests/comptes.test.tsx"
      provides: "le test de visibilite permanente de `Par magasin` et les deux tests de la confirmation de reinitialisation"
      contains: "PERM-03"
    - path: "web/src/pages/comptes/LigneDroit.tsx"
      provides: "`Par magasin` sans revelation au survol, visuellement subordonne"
      contains: "Par magasin"
    - path: "web/src/pages/comptes/dialogues.tsx"
      provides: "DialogueReinitialisation, bati comme les trois confirmations existantes"
      contains: "DialogueReinitialisation"
    - path: "web/src/pages/comptes/DetailCompte.tsx"
      provides: "le bouton ouvre la confirmation ; la mutation part depuis la confirmation seule"
      contains: "reinitialisation"
  key_links:
    - from: "web/src/pages/comptes/DetailCompte.tsx"
      to: "web/src/pages/comptes/dialogues.tsx"
      via: "le meme aiguillage `Confirmation` que la desactivation, le retrait de magasin et l'uniformisation"
      pattern: "DialogueReinitialisation"
    - from: "web/src/pages/comptes/dialogues.tsx"
      to: "RETOUR"
      via: "la constante unique du fichier ; le mot `Annuler` reste absent de ce fichier"
      pattern: "RETOUR"
---

<objective>
Deux defauts trouves par le proprietaire **dans un vrai navigateur**, sur l'ecran
`Comptes et droits`, pendant que 303 tests automatises etaient verts.

1. **`Par magasin` est invisible tant qu'on ne survole pas exactement la bonne ligne.**
   `LigneDroit.tsx:312` porte `opacity-0 ... group-hover:opacity-100
   group-focus-within:opacity-100 focus:opacity-100`. Le controle est indecouvrable, et il
   est **absent purement et simplement au toucher** — ce qui compte pour une tablette de
   comptoir. Le proprietaire en a conclu que l'octroi par magasin n'existait pas. C'etait la
   deviation 3 signalee au plan 03-14, tranchee ici : **le rendre visible en permanence.**

2. **`Reinitialiser le mot de passe` part au clic, sans confirmation et sans retour.**
   `DetailCompte.tsx:370` appelle `reinitialisation.mutate(...)` directement depuis
   `onClick`. Un clic par megarde invalide instantanement le mot de passe du gerant, et
   contrairement aux bascules de droits il n'y a **aucun toast `Annuler`** : l'ancien mot de
   passe est simplement perdu. `DialogueMotDePasse` n'est pas une confirmation — il AFFICHE
   le mot de passe deja genere. **Confirmer d'abord, en nommant la consequence.**

Purpose: rendre decouvrable la fonctionnalite qui porte CLAUDE.md #13, et retirer d'un ecran
de parametres la seule action irreversible qui s'execute sans demander.
Output: trois tests nommes, ecrits d'abord et rouges d'abord, et deux corrections
strictement cote client.

**Ordre impose — test d'abord.** `.planning/TESTING.md` section 1. Les tests existants sont
restes verts au-dessus des deux defauts ; les nouveaux doivent etre **rouges contre le code
actuel**, et le rouge observe doit etre consigne dans le SUMMARY. Un test ecrit apres la
correction est un test ecrit pour passer.

**Ce qui ne bouge pas, et ne doit pas bouger.** CLAUDE.md #13 est une non-negociable
enregistree : la case a cocher **defaut uniforme** a travers les magasins accordes, la
sous-liste par magasin **sur demande seulement**, et le stockage par
(gerant, magasin, permission). Rendre le bouton visible ne deplie aucune ligne et ne change
aucune forme de donnee. Si une correction demandait l'un des deux, s'arreter et demander.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/TESTING.md
@web/src/pages/comptes/LigneDroit.tsx
@web/src/pages/comptes/DetailCompte.tsx
@web/src/pages/comptes/dialogues.tsx
@web/tests/comptes.test.tsx
@.planning/phases/03-comptes-permissions-app-shell/03-UI-SPEC.md

<interfaces>
Tout ce qui suit a ete verifie dans le depot. Aucune exploration a faire.

**`web/tests/comptes.test.tsx`** — harnais deja en place, a reutiliser :

- `rendreLaFiche({ compte?, droits?, magasinsAccordes?, catalogue?, amorcage?, surDroits?,
  surMagasins?, surUniformiser?, surStatut?, surJournal? })` monte l'application reelle sur
  `/parametres/comptes/2`. Elle **n'a pas** d'entree pour `POST /api/comptes/2/mot-de-passe/`,
  et le stub `fetch` **jette** sur un chemin non pose : il faut donc ajouter une option
  `surMotDePasse`, dans la meme forme que `surStatut`.
- `catalogueOffrable([ANFA, MAARIF])` rend **5 droits** en 3 sections : `stock.voir`,
  `stock.ajuster`, `caisse.voir`, `caisse.saisir`, `compte.gerer`.
- `ligneKarim()` : id 2, `nom_complet: "Karim Benali"`, `est_proprietaire: false`. Le prenom
  rendu par `prenomDe` est donc **`Karim`**.
- `appels` est la liste des requetes observees (`{methode, chemin, corps}`), remise a zero par
  le `beforeEach`. C'est l'outil de l'assertion « rien n'est parti ».
- `DROITS_MIXTES` rend `caisse.saisir` mixte, donc **deplie** : cette ligne affiche
  `Uniformiser` et non `Par magasin`. Un test de visibilite de `Par magasin` doit donc partir
  de droits **uniformes**, ou compter en tenant compte de la ligne depliee.
- Le `beforeEach` purge les toasts `sonner` : ne pas s'en soucier.

**`web/src/pages/comptes/LigneDroit.tsx`** :

- `<li className="group border-b border-border py-3 last:border-0">` — le `group` n'existe
  **que** pour les `group-hover:` / `group-focus-within:` du bouton. Il devient mort une fois
  la revelation retiree.
- Le bouton fautif est la branche `deplie === false` de `surchargeable ?`, ligne 305-316. La
  branche `deplie === true` rend `Uniformiser`, **deja visible en permanence** — c'est la
  reference de poids visuel a egaler.
- `surchargeable = magasinsAccordes.length >= 2 && surBasculeDunMagasin !== undefined` :
  **ne pas y toucher**, c'est la regle qui protege l'entreprise mono-magasin.
- `const deplie = choix ?? etat === "mixte";` : **ne pas y toucher**, c'est le defaut uniforme
  de CLAUDE.md #13.

**`web/src/pages/comptes/dialogues.tsx`** :

- `const RETOUR = "Retour";` — l'unique libelle secondaire. Le mot `Annuler` est **banni de ce
  fichier** par son bloc d'en-tete, et un `grep` de ce mot dans ce fichier doit rester vide.
- Trois confirmations deja batties sur `AlertDialog` : `DialogueDesactivation`,
  `DialogueRetraitMagasin`, `DialogueUniformisation`. Forme commune :
  `<AlertDialog open={ouvert} onOpenChange={(etat) => (etat ? undefined : surRetour())}>`,
  `AlertDialogTitle`, `AlertDialogDescription`, puis
  `<AlertDialogCancel onClick={surRetour}>{RETOUR}</AlertDialogCancel>` et
  `<AlertDialogAction onClick={surConfirmation}>`.
- Les deux dialogues **destructifs** (7.9) portent
  `className="bg-destructive text-white hover:bg-destructive/90"` sur l'action ;
  `DialogueUniformisation`, qui **remplace** des reglages sans rien detruire, ne la porte pas.

**`web/src/pages/comptes/DetailCompte.tsx`** :

- `type Confirmation` est une union discriminee sur `quoi`, avec trois membres. L'aiguillage
  est `setConfirmation({ quoi: ... })` d'un cote, `ouvert={confirmation?.quoi === "..."}` de
  l'autre. **C'est le motif a suivre**, pas un nouveau.
- `prenomDe(compte.nom_complet)` est deja calcule dans `const prenom`.
- `reinitialisation` est la mutation (`POST /api/comptes/{id}/mot-de-passe/`) ; son
  `onSuccess` pose `setSecret(...)`, ce qui ouvre `DialogueMotDePasse`. **Ce chemin ne change
  pas** : il est seulement deplace derriere la confirmation.
- Les trois autres confirmations font `setConfirmation(null)` en **premiere ligne** de leur
  fonction d'action (`uniformiser`, `retirerUnMagasin`, `changerLeStatut`). A imiter.

**Copie exacte decidee par le proprietaire** (defaut 2) :

- Titre : `Réinitialiser le mot de passe de Karim ?` — le **prenom**, comme
  `DialogueRetraitMagasin` et `DialogueUniformisation`.
- Corps : `Son mot de passe actuel cessera immédiatement de fonctionner. Vous devrez lui communiquer le nouveau.`
- Actions : `Retour` et `Réinitialiser`.

Accents obligatoires (9.2) : `Réinitialiser`, `immédiatement`, `devrez`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `Par magasin` visible en permanence (PERM-03)</name>
  <files>web/tests/comptes.test.tsx, web/src/pages/comptes/LigneDroit.tsx</files>
  <behavior>
    Un test, dans le `describe("la surcharge par magasin")`, place **juste apres**
    `n'offre \`Par magasin\` qu'a partir de deux magasins` dont il est le pendant :

    `it("rend \`Par magasin\` visible sans survol ni focus, sur chaque ligne (PERM-03)")`

    - `rendreLaFiche()` avec des droits **uniformes** sur les deux magasins, de sorte
      qu'aucune ligne ne soit depliee : par exemple `stock.voir` et `caisse.voir` actifs dans
      `["ANFA", "MAARIF"]`.
    - **Aucun `fireEvent.mouseOver`, `fireEvent.focus`, `hover()` ni `.focus()` nulle part dans
      ce test.** C'est le point : le controle doit exister a l'ecran tel qu'il est rendu.
    - Assertion 1 — presence sur **les cinq** lignes du catalogue de test :
      `expect(screen.getAllByRole("button", { name: "Par magasin" })).toHaveLength(5)`.
    - Assertion 2 — **celle qui mord**, et la seule qui serait tombee contre le code actuel.
      Sur chaque bouton, aucune classe de revelation :

      ```ts
      for (const bouton of boutons) {
        const classes = bouton.className.split(/\s+/);
        expect(classes).not.toContain("opacity-0");
        expect(
          classes.filter(
            (classe) =>
              classe.startsWith("group-hover:") ||
              classe.startsWith("group-focus-within:") ||
              classe.startsWith("focus:opacity"),
          ),
        ).toEqual([]);
      }
      ```

    - Assertion 3 — **le garde-fou CLAUDE.md #13** : rendre le bouton visible ne deplie rien.
      Aucun sous-interrupteur par magasin n'est monte :
      `expect(screen.queryAllByRole("switch", { name: /—/ })).toHaveLength(0)`.
      (Le tiret cadratin est ce que `SousListeParMagasin` met dans le nom accessible.)

    Docstring / commentaire du test, en francais, portant ce qu'aucune assertion ne dit :
    **pourquoi une assertion de classes et non de visibilite.** jsdom ne calcule pas le CSS —
    `getComputedStyle` ne resout ni Tailwind ni une regle de feuille externe — donc
    `toBeVisible()` de jest-dom rendrait vrai sur un `opacity-0`. L'assertion porte donc sur
    **l'absence des classes de revelation**, ce qui est une approximation assumee : elle
    attrape la regression exacte qui vient de se produire, elle n'est pas une preuve de
    visibilite reelle. La preuve de visibilite reste la traversee humaine au navigateur.
  </behavior>
  <implementation>
    Dans `LigneDroit.tsx`, sur la branche `deplie === false` :

    - Remplacer
      `className="text-xs opacity-0 transition-opacity group-hover:opacity-100 group-focus-within:opacity-100 focus:opacity-100"`
      par `className="text-xs text-muted-foreground"`.
    - **Le poids visuel** : le bouton reste secondaire a l'interrupteur et a son libelle. Il
      garde `variant="ghost"` et `size="sm"`, garde `text-xs` (le libelle du droit est
      `text-sm`), et gagne `text-muted-foreground` — la meme couleur que l'explication de la
      ligne, donc il se lit comme du texte de service et non comme une action principale.
      `Uniformiser`, lui, garde sa couleur pleine : il remplace des reglages poses a la main,
      il merite plus de poids que l'invitation a en poser.
    - Retirer `group` de `<li className="group border-b ...">` : plus aucun `group-*` ne
      subsiste dans ce fichier, et une classe `group` orpheline invite a recoller un
      `group-hover:` un jour.
    - Remplacer le commentaire « Rendu seulement au survol ou au focus de la ligne... » par
      celui qui dit la verite nouvelle, et **pourquoi** : un controle revele au survol est
      indecouvrable — il faut pointer exactement la bonne ligne pour apprendre qu'il existe —
      et il n'existe pas du tout au toucher, donc pas du tout sur la tablette du comptoir. Le
      proprietaire en a conclu que l'octroi par magasin n'existait pas. Ce qui limite
      l'encombrement n'est pas la revelation, c'est `magasins.length >= 2` : une entreprise
      mono-magasin ne voit toujours rien.
    - Ne toucher ni a `surchargeable`, ni a `deplie`, ni a `SousListeParMagasin`.
  </implementation>
  <action>
    1. Ecrire le test. Le lancer : `npm --prefix web test -- -t "Par magasin"`.
       **Verifier que le rouge est le bon rouge** : l'echec doit etre l'assertion 2 (la classe
       `opacity-0` presente), pas l'assertion 1. Consigner le message exact pour le SUMMARY.
       Si le rouge est l'assertion 1, le test ou la fixture est faux — le corriger, et ne pas
       corriger le produit pour le faire passer.
    2. Appliquer la correction de `LigneDroit.tsx`.
    3. Relancer, puis la suite web entiere.
    Commit en nommant exactement `web/tests/comptes.test.tsx` et
    `web/src/pages/comptes/LigneDroit.tsx`. Aucune attribution Claude dans le message.
  </action>
  <verify>
    <automated>npm --prefix web test 2>&1 | tail -8</automated>
  </verify>
  <done>Le test existe ; il a ete rouge sur l'assertion de classes contre le code actuel ; il est vert apres. `Par magasin` n'a plus aucune classe de revelation, `group` a disparu du `li`, `surchargeable` et `deplie` sont inchanges, et le test de l'entreprise mono-magasin reste vert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: confirmer avant de reinitialiser un mot de passe (PERM-02)</name>
  <files>web/tests/comptes.test.tsx, web/src/pages/comptes/dialogues.tsx, web/src/pages/comptes/DetailCompte.tsx</files>
  <behavior>
    D'abord etendre le harnais : ajouter a `rendreLaFiche` une option
    `surMotDePasse?: (requete: Request) => Response | Promise<Response>` posee sur
    `table["POST /api/comptes/2/mot-de-passe/"]`, exactement dans la forme de `surStatut`.
    Sans elle le stub `fetch` jette sur ce chemin.

    Puis deux tests, dans `describe("l'annulation, les echecs et les dialogues")`, apres
    `porte la copie exacte du dialogue de retrait d'un magasin` :

    **1. `it("ne reinitialise aucun mot de passe avant confirmation, et le \`Retour\` laisse le compte intact (PERM-02)")`**

    - `rendreLaFiche({ surMotDePasse: () => json({ mot_de_passe_provisoire: "Tr0is-Mots-Ici" }) })`.
    - Cliquer `Réinitialiser le mot de passe`.
    - **L'assertion qui mord** :
      `expect(appels.some((appel) => appel.chemin === "/api/comptes/2/mot-de-passe/")).toBe(false)`.
      Contre le code actuel elle est fausse — la mutation part depuis le `onClick`.
    - La copie exacte, dans l'esprit des deux tests de dialogue voisins :
      - `screen.getByRole("heading", { name: "Réinitialiser le mot de passe de Karim ?" })` ;
      - `screen.getByText("Son mot de passe actuel cessera immédiatement de fonctionner. Vous devrez lui communiquer le nouveau.")` ;
      - `screen.getByRole("button", { name: "Retour" })` present ;
      - `expect(screen.queryByRole("button", { name: "Annuler" })).toBeNull()` — 7.10.
    - Cliquer `Retour`, puis : toujours aucun appel a `/api/comptes/2/mot-de-passe/`, et
      **aucun mot de passe provisoire a l'ecran** —
      `expect(screen.queryByText("Tr0is-Mots-Ici")).toBeNull()`.

    **2. `it("reinitialise apres confirmation, et montre le mot de passe provisoire (PERM-02)")`**

    - Meme montage. Cliquer `Réinitialiser le mot de passe`, puis, **dans le dialogue**,
      `Réinitialiser`.
    - `await waitFor(...)` : un `POST /api/comptes/2/mot-de-passe/` figure dans `appels`.
    - `expect(await screen.findByText("Tr0is-Mots-Ici")).toBeTruthy()` — `DialogueMotDePasse`
      s'ouvre comme aujourd'hui, et son `Fermer` est intact.
    - Piege a eviter : le bouton de la fiche s'appelle `Réinitialiser le mot de passe` et
      l'action du dialogue `Réinitialiser`. `getByRole("button", { name: "Réinitialiser" })`
      est un **nom accessible exact** en Testing Library, donc il ne prend pas le premier ;
      mais l'ordre des clics doit rester lisible — commenter le point.
  </behavior>
  <implementation>
    **`dialogues.tsx`** — ajouter `DialogueReinitialisation`, bati comme
    `DialogueUniformisation`, **pas un nouveau motif** :

    ```tsx
    export type ProprietesDialogueReinitialisation = {
      ouvert: boolean;
      prenom: string;
      surRetour: () => void;
      surConfirmation: () => void;
    };
    ```

    Titre `Réinitialiser le mot de passe de {prenom} ?`, description
    `Son mot de passe actuel cessera immédiatement de fonctionner. Vous devrez lui communiquer le nouveau.`,
    `AlertDialogCancel` → `{RETOUR}`, `AlertDialogAction` → `Réinitialiser`.

    **Pas de `bg-destructive`** sur l'action, et le commentaire doit le dire : l'action
    **remplace** une identification, elle ne detruit aucune donnee et ne ferme aucune porte —
    le nouveau mot de passe est delivre dans le meme geste. C'est le cas de
    `DialogueUniformisation`, pas celui des deux dialogues de 7.9. Peindre en rouge une action
    de routine du support use le rouge des deux qui en ont besoin.

    Un commentaire de tete expliquant **pourquoi ce dialogue existe alors que 7.9 n'en nomme
    que deux** : `DialogueMotDePasse` AFFICHE un mot de passe deja genere, il ne demande rien ;
    l'action, elle, est la seule de l'ecran qui soit a la fois immediate et sans retour — les
    vingt et une bascules ont leur toast `Annuler` (7.8), la desactivation et le retrait de
    magasin ont leur confirmation, et celle-ci n'avait ni l'un ni l'autre. Un clic par megarde
    coupait l'acces d'un gerant sans que rien ne le dise.

    **`DetailCompte.tsx`** :

    - `type Confirmation` gagne `| { quoi: "reinitialisation" }`.
    - Le `onClick` du bouton devient `() => setConfirmation({ quoi: "reinitialisation" })`.
    - Une fonction `reinitialiserLeMotDePasse` a cote de `uniformiser` / `retirerUnMagasin`,
      commencant par `setConfirmation(null)`, puis contenant **exactement** le
      `reinitialisation.mutate(...)` d'aujourd'hui, `onSuccess: setSecret(...)` compris.
      Deplacer ce corps, ne pas le reecrire.
    - Monter `<DialogueReinitialisation ouvert={confirmation?.quoi === "reinitialisation"}
      prenom={prenom} surRetour={() => setConfirmation(null)}
      surConfirmation={reinitialiserLeMotDePasse} />` a cote des trois autres, **avant**
      `DialogueMotDePasse` — l'ordre de lecture du fichier suit l'ordre d'apparition a
      l'ecran.
    - Ne rien changer au serveur. Si un fichier Python, `web/src/api/schema.yml` ou
      `types.gen.ts` se retrouve modifie, c'est une erreur : le revenir.
  </implementation>
  <action>
    1. Ajouter `surMotDePasse` au harnais et ecrire les deux tests. Lancer
       `npm --prefix web test -- -t "PERM-02"`. **Verifier le rouge** : le test 1 doit tomber
       sur l'assertion « aucun appel » (ou sur le titre du dialogue introuvable), c'est-a-dire
       sur le tir immediat de la mutation. Consigner le message exact pour le SUMMARY.
    2. Appliquer les corrections.
    3. Relancer la suite web entiere, puis `npm --prefix web run build` et
       `npm --prefix web run audit:format`.
    4. Verifier que `Annuler` reste absent de `dialogues.tsx` :
       `! grep -q "Annuler" web/src/pages/comptes/dialogues.tsx`.
    Commit en nommant exactement `web/tests/comptes.test.tsx`,
    `web/src/pages/comptes/dialogues.tsx` et `web/src/pages/comptes/DetailCompte.tsx`. Aucune
    attribution Claude dans le message.
  </action>
  <verify>
    <automated>npm --prefix web test 2>&1 | tail -8 && npm --prefix web run build 2>&1 | tail -5 && npm --prefix web run audit:format && ! grep -q "Annuler" web/src/pages/comptes/dialogues.tsx && git status --short</automated>
  </verify>
  <done>Les deux tests existent, ont ete rouges contre le code actuel, sont verts apres. Aucun appel reseau ne part avant la confirmation ; `Retour` laisse le compte intact ; `Réinitialiser` produit le meme comportement qu'avant. `Annuler` reste absent de `dialogues.tsx`. Aucun fichier Python ni schema ne bouge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proprietaire → interface d'octroi | Ce que l'ecran montre determine ce que le proprietaire croit pouvoir faire ; une fonctionnalite invisible est une fonctionnalite absente |
| proprietaire → identifiants d'un gerant | Une action irreversible declenchee par un clic unique, sur un ecran dense de controles reversibles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Ql7l-01 | Elevation of privilege | `LigneDroit.tsx` `Par magasin` | mitigate | Le controle invisible pousse a l'octroi uniforme par defaut : le proprietaire accorde partout parce qu'il ignore qu'il peut accorder ici seulement. C'est une elevation par ergonomie, et elle contourne l'intention de CLAUDE.md #13 sans contredire une ligne de code. Le rendre visible est la mitigation. |
| T-Ql7l-02 | Denial of service | `DetailCompte.tsx` reinitialisation | mitigate | Un clic par megarde coupe l'acces d'un gerant en plein service, sans retour possible : le mot de passe precedent n'existe plus. La confirmation qui nomme la consequence est la mitigation ; elle n'est pas cosmetique, c'est la seule barriere entre une souris qui derape et un comptoir a l'arret. |
| T-Ql7l-03 | Tampering | `LigneDroit.tsx` `deplie` | mitigate | Le risque du correctif est de « rendre visible » en depliant : cela renverserait le defaut uniforme de CLAUDE.md #13 et poserait 21 × N controles a l'ecran. L'assertion 3 de la tache 1 — aucun sous-interrupteur monte — est le garde-fou qui rend cette solution rouge. |
| T-Ql7l-04 | Repudiation | `dialogues.tsx` lexique | accept | Employer `Annuler` au lieu de `Retour` ferait croire a une annulation d'octroi (menace T-03-102 deja enregistree). Couvert par le `grep` d'absence sur le fichier, deja en place. |
</threat_model>

<verification>
- `npm --prefix web test` — **119 attendus avant, 122 apres** (3 tests ajoutes). Les 119
  existants restent verts : aucun ne depend de la revelation au survol ni du tir immediat.
- Le rouge de chaque tache a ete observe **avant** sa correction, et son message est consigne
  dans le SUMMARY. C'est la preuve test-first, et ces deux tests-la valent surtout par ce
  rouge : leurs voisins etaient verts au-dessus des memes defauts.
- `npm --prefix web run build` — sortie 0 (`tsc -b`, `tsc -p tsconfig.test.json`, `vite build`).
- `npm --prefix web run audit:format` — aucune sortie.
- `grep -c "Annuler" web/src/pages/comptes/dialogues.tsx` — 0.
- `git status --short` ne montre **aucun** fichier Python, ni `web/src/api/schema.yml`, ni
  `web/src/api/types.gen.ts`. Aucun changement serveur n'est attendu, donc aucune
  regeneration de schema.
- Suite backend : **non relancee par ce lot**, aucun fichier Python ne bouge. Baseline connue
  184 passed / 30 deselected ; la relancer une fois en fin de lot pour le constater.
</verification>

<success_criteria>
- `Par magasin` est rendu sans aucune classe de revelation, sur chaque ligne d'un compte a 2+
  magasins, et l'entreprise mono-magasin n'en voit toujours aucun.
- Aucune ligne n'est depliee par defaut ; `surchargeable` et `deplie` sont inchanges ; le
  stockage n'est pas touche — CLAUDE.md #13 intact.
- `Réinitialiser le mot de passe` n'envoie rien avant confirmation ; `Retour` laisse le compte
  intact ; la confirmation porte la copie exacte decidee par le proprietaire.
- Les trois tests ont ete rouges contre le code d'avant, et le rouge observe est consigne.
- 122 tests web verts, build a 0, `audit:format` muet.
- Deux commits, un par defaut, nommant leurs chemins, sans aucune attribution Claude.
- STATE.md « Quick Tasks Completed » mis a jour ; ROADMAP.md non touche.
</success_criteria>

<output>
Apres completion, creer
`.planning/quick/260917-l7l-rendre-l-octroi-par-magasin-visible-et-c/260917-l7l-SUMMARY.md`.

Y consigner explicitement :
- le **rouge observe** pour chaque tache — assertion concernee et message — c'est la preuve
  test-first, et ici la preuve que les tests mordent la ou leurs voisins ne mordaient pas ;
- ce qui a ete fait du **poids visuel** du bouton desormais permanent ;
- la **limite honnete** de l'assertion de la tache 1 : jsdom ne calcule pas le CSS, c'est une
  assertion de classes et non une assertion de visibilite ;
- **la classe de defauts que la suite ne peut structurellement pas voir.** Ces deux defauts,
  le 403 CSRF d'origine et l'ecran de parametres inatteignable ont tous ete trouves par un
  humain dans un navigateur pendant que 303 tests etaient verts. Nommer la classe, pour les
  phases 4 a 12.
</output>
