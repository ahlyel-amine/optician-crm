---
phase: 04-clients-ordonnances
plan: 09
type: execute
wave: 6
depends_on: [05, 06, 07, 08]
files_modified:
  - web/src/pages/clients/FicheClient.tsx
  - web/src/pages/clients/OngletsDuClient.tsx
  - web/src/pages/clients/ordonnances/HistoriqueOrdonnances.tsx
  - web/src/pages/clients/ordonnances/CarteVersion.tsx
  - web/src/pages/clients/ordonnances/PhotoOrdonnance.tsx
  - web/src/pages/clients/ordonnances/messages.ts
  - web/src/pages/clients/ordonnances/SaisieOrdonnance.tsx
  - web/src/print.css
  - web/src/App.tsx
  - web/tests/ordonnance-historique.test.tsx
  - web/tests/clients.test.tsx
autonomous: false
requirements: [CLIENT-02, CLIENT-06, CLIENT-09]
user_setup: []

must_haves:
  truths:
    - "On atteint l'historique d'un client en CLIQUANT : racine, Clients, une ligne, l'onglet Ordonnances"
    - "Une version enregistree se rend telle qu'elle a ete saisie — l'affichage ne la revalide JAMAIS"
    - "Aucun controle de modification ni de suppression n'existe sur une ordonnance, nulle part dans l'interface"
    - "Rien n'est rouge : une version remplacee n'est pas une erreur"
    - "La feuille imprimee porte `Cylindre negatif.` et aucune photo"
    - "L'onglet `Achats` n'est PAS rendu, et CLIENT-02 n'est pas cochee"
    - "La photo s'attache une fois ; le controle dit qu'elle ne se remplace pas"
  artifacts:
    - path: "web/src/pages/clients/ordonnances/CarteVersion.tsx"
      provides: "une version, rendue par LigneDeCorrection, sans revalidation ni couleur"
      contains: "LigneDeCorrection"
    - path: "web/src/pages/clients/FicheClient.tsx"
      provides: "coordonnees en edition sur place, et le resume protege"
      contains: "resume_ordonnance"
    - path: "web/src/print.css"
      provides: "un bloc documentaire de plus, aucune geometrie de page"
      contains: "ordonnance"
    - path: "web/tests/ordonnance-historique.test.tsx"
      provides: "le parcours par clics, l'absence de controle de modification, la non-revalidation"
      contains: "Supprimer"
  key_links:
    - from: "web/src/pages/clients/FicheClient.tsx"
      to: "web/src/pages/clients/ordonnances/LigneDeCorrection.tsx"
      via: "UN composant rend une correction, partout"
      pattern: "LigneDeCorrection"
    - from: "web/src/pages/clients/OngletsDuClient.tsx"
      to: "web/src/auth/RequirePermission.tsx"
      via: "l'onglet Ordonnances est ABSENT sans le droit, jamais grise"
      pattern: "ordonnance.voir"
---

<objective>
Les surfaces de lecture du dossier client : la fiche, l'historique des versions,
l'impression, et la photo.

Purpose: CLIENT-06 se rend ici **littéralement**, et la phrase qui gouverne l'écran est
celle-ci :

> **Une version enregistrée se rend telle qu'elle a été saisie. L'affichage ne la revalide
> jamais.**

Les bornes vivent à un endroit servi et le propriétaire les a déjà changées une fois. Une
version portant une valeur que les bornes d'aujourd'hui refuseraient doit donc s'afficher
**sans avertissement, sans badge d'erreur, sans annotation** — sinon les anciennes fiches
paraîtraient fausses parce que la règle a bougé, ce qui détruit exactement la garantie que
CLIENT-06 achète. Les notes `À vérifier` sont une propriété du **formulaire de saisie**,
jamais d'une ordonnance stockée.

Et la seconde règle, qui se vérifie par une absence : **il n'existe aucun contrôle de
modification et aucun contrôle de suppression sur une ordonnance, nulle part.** Le mot
`Supprimer` n'apparaît pas sur ces écrans, exactement comme il n'apparaît pas dans
l'interface des comptes. Corriger, c'est `Saisir une ordonnance` →
`Correction d'une saisie précédente` → un `Motif`.

Output: quatre composants, un bloc d'impression, et la passe humaine de la phase — **cinq
étapes, pas six**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-clients-ordonnances/04-UI-SPEC.md
@web/src/pages/clients/ListeClients.tsx
@web/src/pages/clients/ordonnances/LigneDeCorrection.tsx
@web/src/pages/clients/ordonnances/SaisieOrdonnance.tsx
@web/src/pages/comptes/DetailCompte.tsx
@web/src/layout/NavSecondaire.tsx
@web/src/etats/messages.ts
@web/src/print.css
@web/src/components/ui/badge.tsx
@web/tests/clients.test.tsx
@web/tests/nomAccessible.ts
</context>

<interfaces>
    // plan 04-08
    export function LigneDeCorrection({correction}) …     // OD +2,00 (−1,00 à 90°) Add. +2,25
    // plan 04-02
    export function nomAccessible(element: Element): string;
    // web/src/etats/messages.ts — réutilisé VERBATIM, aucun écran de la phase 4 n'écrit sa propre copie
    export function messageChargementImpossible(quoi: string): string;
    // plan 04-06, côté serveur
    GET  /api/ordonnances/<id>/photo/    → FileResponse, derrière ordonnance.voir
    POST /api/ordonnances/<id>/photo/    → attache, une seule fois, derrière ordonnance.saisir

**Les onglets de dossier ne sont PAS dans le tableau `NAV`, et c'est écrit pour qu'un
relecteur ne le « corrige » pas.** `03` §5.3 pose que le second niveau vit dans le même
tableau que le premier, et la règle est juste — **pour des sections**. Ces onglets-ci sont
liés à un **enregistrement** : leurs routes portent un `:id` et leur visibilité dépend d'un
droit que le prédicat de `NAV` connaît déjà, mais sur une **ressource** qu'il ne connaît
pas. Les y forcer mettrait une route paramétrée dans un tableau de constantes. Ce qui **est**
hérité, et c'est ce qui comptait, c'est le prédicat : une entrée dont le code n'est pas
détenu est **absente**, jamais grisée, et il n'y a qu'un seul filtre.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: la fiche client et l'historique, rouges d'abord puis construits</name>
  <files>web/tests/ordonnance-historique.test.tsx, web/src/pages/clients/FicheClient.tsx, web/src/pages/clients/OngletsDuClient.tsx, web/src/pages/clients/ordonnances/HistoriqueOrdonnances.tsx, web/src/pages/clients/ordonnances/CarteVersion.tsx, web/src/App.tsx</files>
  <behavior>
    **Six `it` dans `web/tests/ordonnance-historique.test.tsx`.**

    1. `on atteint l_historique en CLIQUANT depuis la racine`
       — monter à `/`, cliquer `Clients`, cliquer une ligne, cliquer l'onglet
       `Ordonnances`, assert une carte de version. **La leçon 03-14, appliquée au parcours
       le plus long de la phase.** C'est aussi le seul test qui prouve que les onglets sont
       branchés sur de vraies routes.
    2. `sans ordonnance.voir, l_onglet est ABSENT et la fiche ne porte pas le resume`
       — deux moitiés : l'onglet n'est pas dans le DOM (absent, pas grisé, pas d'infobulle),
       et la charge utile sans `resume_ordonnance` ne rend **aucune** carte `Dernière
       ordonnance` — **sans aucune branche cliente**, uniquement par l'absence de la clé.
       Moitié positive : avec la clé, la carte est rendue.
    3. `aucun controle de modification ni de suppression n_existe`
       — `queryAllByText` sur `Supprimer`, `Modifier`, `Éditer` rend **zéro** sur la fiche,
       sur l'historique et sur la carte de version. Contrôle positif dans le même test :
       `Saisir une ordonnance` **est** présent — sans quoi un écran vide passerait.
    4. `une version stockee ne se revalide pas a l_affichage`
       — une carte dont la sphère est hors des bornes servies rend la valeur **et aucune
       note `À vérifier`, aucun badge d'erreur, aucune classe destructive**.
       **Ce qu'il attrape :** la réutilisation de `verifier.ts` sur un écran de lecture,
       qui est l'erreur naturelle puisque le module est juste à côté.
    5. `rien n_est rouge sur une version remplacee`
       — la liste de classes de la carte `Remplacée` ne contient aucun jeton destructif, et
       le badge porte **le mot**. Assertion **de classe**, explicitement étiquetée comme
       telle dans le nom du test : jsdom ne calcule aucun CSS, donc « n'est pas rouge » est
       une revendication de jeton, pas de pixel. La revendication visuelle est la
       vérification manuelle n° 2.
    6. `l_onglet Achats n_est pas rendu`
       — assertion d'absence, avec un commentaire disant pourquoi ce n'est pas un oubli.
  </behavior>
  <action>
**A. `OngletsDuClient.tsx`** — `Fiche` toujours ; `Ordonnances` sous `ordonnance.voir`,
via `RequirePermission` donc **absent** sans le droit ; **`Achats` jamais**.

`04-UI-SPEC.md` §19.4 refuse de livrer un onglet vrai et inutile, et le raisonnement se
recopie dans le composant : CLIENT-02 (« voir l'historique d'achats complet d'un client »)
et le critère 1 de la feuille de route ont besoin des **ventes, qui sont la phase 6**. Il
n'y a rien à montrer et il ne peut rien y avoir. C'est l'argument `disponible: false` de
`03` §5.3, un étage plus bas : un opticien qui clique `Achats` et trouve un `Aucun achat`
permanent a appris que le produit est cassé.

**La phase 4 livre donc la fiche avec l'emplacement et sans l'onglet, et CLIENT-02 reste
non cochée** — même discipline que 03-05..03-10 qui n'ont pas coché PERM-02/03 avant
l'existence de leur moitié interface, et que `04-CONTEXT.md` applique à CLIENT-08.
**Ne pas déplacer CLIENT-02 dans `REQUIREMENTS.md`** : une exigence reste rattachée à la
phase qui construit sa structure, et n'est cochée que par celle où un opticien peut
réellement l'accomplir. La déplacer perdrait la trace de qui a posé les fondations.

**B. `FicheClient.tsx`.** Largeur de contenu 880px. Titre = le nom du client, en `<bdi>`.
Sous-titre discret `Client depuis le 14/03/2025.`

*Région A — Coordonnées*, en édition sur place comme `03` §7.3A : enregistrement par champ
au blur, l'échec rendu **sur ce champ** et jamais en toast. Sans `client.modifier`, les
champs se rendent en valeurs statiques — **pas en entrées désactivées**.

*Région B — Dernière ordonnance* : une carte, présente **uniquement** quand
`resume_ordonnance` est dans la charge utile. Rendue par `LigneDeCorrection`, le **même**
composant que le panneau de relecture, l'historique et l'impression. Une correction rendue
de deux façons finira par être rendue de deux façons différentes, et le lecteur n'aura
aucun moyen de savoir laquelle correspond au papier.

`Saisir une ordonnance` n'apparaît qu'avec `ordonnance.saisir`. `Voir l'historique` mène à
l'onglet.

**C. `HistoriqueOrdonnances.tsx` et `CarteVersion.tsx`.** Plus récente d'abord, une carte
par version, 880px, 24px d'écart. Le contenu exact de §21.1.

Badges **neutres portant le mot** (`03` §4) : `Version en cours`, `Remplacée`, `Correction`,
`Renouvellement`, `Ordonnance médicale`, `Réfraction opticien`. **Rien n'est rouge** — une
version remplacée n'est pas une erreur, et la phase 4 n'introduit aucune couleur.

`Remplace la version 2` est un lien qui défile jusqu'à cette carte **et lui donne le focus**.
La ligne de provenance nomme le magasin — provenance, jamais filtre (D-4a).

État vide : `Aucune ordonnance` / `Saisissez l'ordonnance du client pour commander ses
verres et déclencher ses rappels.`

**Aucun contrôle de modification, aucun contrôle de suppression.** `CarteVersion` ne rend
ni menu de ligne, ni icône crayon, ni corbeille. Corriger, c'est une nouvelle version avec
son motif : `Dites ce qui était faux. Cela restera lisible.`

`/clients/:id/ordonnances/:version` rend **la même carte** en pleine largeur, pour le lien
et pour l'impression.

**Pas de vue de différence rétrospective entre deux versions en phase 4**, et la raison est
écrite : le moment où une comparaison évite une erreur est **pendant la saisie**, et il est
couvert par le panneau de relecture et la colonne de référence. Un diff est un confort ;
différé avec sa raison.

Monter les routes dans `web/src/App.tsx`.

Lancer `npm --prefix web test` — rouge d'abord, puis vert — et `npm --prefix web run build`.

Commit (chemins nommés, aucune mention de Claude) :
`git add web/tests/ordonnance-historique.test.tsx web/src/pages/clients/FicheClient.tsx web/src/pages/clients/OngletsDuClient.tsx web/src/pages/clients/ordonnances/HistoriqueOrdonnances.tsx web/src/pages/clients/ordonnances/CarteVersion.tsx web/src/App.tsx && git commit -m "feat(04-09): la fiche client et l'historique des versions"`
  </action>
  <verify>
    <automated>npm --prefix web test 2>&amp;1 | tail -8</automated>
    <automated>npm --prefix web run build</automated>
    <automated>! grep -rnE '&gt;(Supprimer|Modifier|Éditer)&lt;' web/src/pages/clients/ordonnances/</automated>
    <automated>! grep -rn 'verifierOrdonnance' web/src/pages/clients/ordonnances/CarteVersion.tsx</automated>
    <automated>! grep -rnE 'destructive' web/src/pages/clients/ordonnances/CarteVersion.tsx</automated>
    <automated>! grep -rn 'Achats' web/src/pages/clients/OngletsDuClient.tsx || grep -q 'phase 6' web/src/pages/clients/OngletsDuClient.tsx</automated>
  </verify>
  <done>On atteint l'historique par quatre clics depuis la racine. Aucun contrôle de modification ni de suppression n'existe, prouvé avec son contrôle positif. Une version stockée n'est jamais revalidée — `verifier.ts` n'est pas importé par la carte. Rien n'est rouge. L'onglet `Achats` n'est pas rendu et le composant dit pourquoi.</done>
</task>

<task type="auto">
  <name>Task 2: la photo, et la feuille imprimée</name>
  <files>web/src/pages/clients/ordonnances/PhotoOrdonnance.tsx, web/src/pages/clients/ordonnances/SaisieOrdonnance.tsx, web/src/pages/clients/ordonnances/messages.ts, web/src/print.css, web/tests/ordonnance-historique.test.tsx</files>
  <action>
**A. Attacher — dans le formulaire de saisie, dernier bloc avant les boutons.**

Un vrai `<input type="file">` avec un `<label>` **visible** `Photo de l'ordonnance` et un
bouton stylé `Ajouter une photo`. Le glisser-déposer est une amélioration ; **le sélecteur
de fichier est le contrat**, et il est atteignable au clavier parce que c'est une vraie
entrée.

`accept="image/jpeg,image/png,image/webp,image/heic"`. **Aucun PDF en phase 4** — un PDF
demande une visionneuse et le chemin documentaire est la phase 9 ; consigné en `D-4-3` de
`deferred-items.md`, pas oublié. Pas d'attribut `capture` : photographier au comptoir est
le travail de l'application Expo de la phase 11.

Aide : `Conservez la photo du papier : elle sert de justificatif pour un remboursement AMO.`

Contrôle de taille **avant** téléversement, 10 Mo :
`Cette image fait 14 Mo. La limite est de 10 Mo.` — en **mégaoctets**, jamais en octets,
jamais un type MIME à l'écran. Mauvais type :
`Ce fichier n'est pas une image. Formats acceptés : JPG, PNG, WEBP, HEIC.`

**Le message d'erreur de téléversement n'échoie JAMAIS le nom du fichier.** Un nom de
fichier téléversé porte couramment le nom du patient (`ordonnance_benali_ahmed.jpg`), et
une chaîne d'erreur est la seule chaîne du produit qui atteint Sentry de façon fiable. Le
serveur a sa moitié (plan 04-06, `SENSITIVE_KEY`) ; celle-ci est la moitié cliente, et un
test l'assert littéralement en posant ce nom-là et en cherchant `benali` dans le DOM.

**B. Consulter.**

Vignette de 96px à côté de la version. `Voir la photo` ouvre un `dialog`, image en
`max-height: 90vh`, `alt=""` et le dialogue **titré** `Photo de l'ordonnance — version 2` :
l'image ne porte aucune information exploitable par un lecteur d'écran, le titre si.

Les octets viennent d'une **route DRF authentifiée** qui a déjà résolu `ordonnance.voir` —
jamais un `MEDIA_URL`, jamais une URL pré-signée. Donc `<img src>` est un chemin d'API, et
un échec rend `messageChargementImpossible("la photo")` depuis
`web/src/etats/messages.ts` — **réutilisé verbatim**, pas réécrit — et non une icône
d'image cassée.

Le nom de fichier s'affiche à côté de la vignette, tronqué à 40 caractères, dans `<bdi>` :
c'est un contrôle de mauvaise pièce jointe utile à qui voit déjà le dossier. **Il
n'apparaît jamais dans un titre de page, un toast ou une erreur.**

**C. Attacher une fois, et le contrôle le dit.**

Une version enregistrée **sans** photo peut en recevoir une plus tard — le papier arrive
souvent le lendemain, et c'est la seule mutation permise sur une ligne immuable (§28-Q5,
tranchée oui). **Jamais de remplacement, jamais de suppression.** Le contrôle porte la
phrase : `Une photo ne se remplace pas. Pour corriger, enregistrez une nouvelle version.`

Test : sur une version portant déjà une photo, **aucun** contrôle d'attache n'est rendu, et
la phrase l'est. Contrôle positif : sur une version sans photo, le contrôle **est** rendu.

**D. La feuille imprimée — un bloc documentaire, aucune géométrie.**

`Imprimer` appelle `window.print()` contre `web/src/print.css`, livré en phase 3 — **aucune
géométrie de page n'est ajoutée ici**, ce qui était tout l'intérêt de l'avoir livré alors.
Phase 4 ajoute **un** bloc :

- en-tête : le nom du client, la `raison_sociale`, le magasin, la version et sa date ;
- les lignes de `LigneDeCorrection`, **le même composant que l'écran** ;
- le pied de page **`Cylindre négatif.`** — une correction imprimée sans convention
  énoncée est la mauvaise paire de verres en attente d'être commandée ;
- noir sur blanc, et **aucune photo** : une photo de téléphone en niveaux de gris coûte de
  l'encre et ne prouve rien ; la photo est une preuve tenue dans le dossier, pas sur le
  ticket du comptoir.

Test : la règle d'impression du pied existe dans `print.css` et le bloc rend la phrase.
**Étiqueter honnêtement ce que le test ne prouve pas** : jsdom n'applique aucune feuille
`@media print`, donc la lisibilité réelle est la vérification manuelle n° 4.

Lancer `npm --prefix web test`, les trois gardes, `npm --prefix web run build`.

Commit (chemins nommés, aucune mention de Claude) :
`git add web/src/pages/clients/ordonnances/PhotoOrdonnance.tsx web/src/pages/clients/ordonnances/SaisieOrdonnance.tsx web/src/pages/clients/ordonnances/messages.ts web/src/print.css web/tests/ordonnance-historique.test.tsx && git commit -m "feat(04-09): la photo attachee une fois, et la feuille imprimee"`
  </action>
  <verify>
    <automated>npm --prefix web test 2>&amp;1 | tail -8</automated>
    <automated>npm --prefix web run build</automated>
    <automated>grep -q 'Cylindre négatif.' web/src/pages/clients/ordonnances/messages.ts</automated>
    <automated>grep -q 'Une photo ne se remplace pas' web/src/pages/clients/ordonnances/messages.ts</automated>
    <automated>grep -q 'messageChargementImpossible' web/src/pages/clients/ordonnances/PhotoOrdonnance.tsx</automated>
    <automated>! grep -rn 'application/pdf' web/src/pages/clients/ordonnances/</automated>
    <automated>npm --prefix web run audit:format &amp;&amp; npm --prefix web run audit:projection &amp;&amp; npm --prefix web run audit:clinique</automated>
    <automated>git diff --quiet HEAD~3 -- web/src/etats/messages.ts &amp;&amp; echo "messages.ts GLOBAL INTOUCHE"</automated>
  </verify>
  <done>L'attache est une vraie entrée fichier avec un label visible, bornée en taille et en type, et son message n'échoie jamais le nom du fichier. La consultation passe par la route authentifiée et réutilise le message d'échec global. Une photo posée ne se remplace pas, et le contrôle le dit. La feuille imprimée porte `Cylindre négatif.` et aucune photo, sans une ligne de géométrie ajoutée.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: la passe humaine — cinq étapes, et pas une de plus</name>
  <what-built>
    Toute la phase 4 est en place et automatisée : la fiche client, la recherche tolérante,
    la saisie d'ordonnance avec sa grille, ses avertissements et son panneau de relecture,
    l'historique versionné, l'impression et la photo. Les suites backend et web sont
    vertes, le build sort à 0, le schéma commité est identique au document servi, et les
    gardes `audit:format`, `audit:projection` et `audit:clinique` passent.

    **Ces cinq étapes ne sont pas une relecture générale.** Chacune porte une revendication
    qu'aucune suite ne peut juger, et la raison est la même pour les cinq : **jsdom ne
    calcule aucun CSS.** Un test peut prouver qu'une note existe, qu'elle porte
    `role="status"`, qu'elle est référencée par `aria-describedby`, qu'elle ne pose pas
    `aria-invalid` et que sa liste de classes ne contient aucun jeton destructif. Il ne
    peut pas prouver qu'elle **se lit** plus discrètement qu'une erreur, que l'anneau de
    focus **se voit**, que la grille de 416px **ne se replie pas** à 1280px, ni qu'une
    feuille imprimée **est lisible**.

    **Le solde de 18 vérifications manuelles de la phase 3 est toujours ouvert. Cette phase
    en ajoute exactement CINQ**, ce qui est le plafond que `04-UI-SPEC.md` §25.5 se fixe
    pour cette raison précise. Ne pas en ajouter une sixième pendant la passe : la noter
    comme un élément différé.
  </what-built>
  <how-to-verify>
    Préparation : `docker compose up -d`, `uv run python manage.py runserver`,
    `npm --prefix web run dev`. Une affaire à **deux magasins** (Anfa, Maârif), un client
    portant **au moins deux versions d'ordonnance**, dont une avec une photo.

    **Éprouver les identifiants par un vrai `POST /api/auth/connexion/` avant de commencer.**
    Les mots de passe de développement ont dérivé **trois fois** en phase 3 et une passe qui
    démarre sur un 400 perd sa première demi-heure.

    1. **À 1280px, la grille, la colonne de référence et le panneau de relecture coexistent
       sans repli.** Ouvrir `/clients/:id/ordonnances/nouvelle` avec une fenêtre à exactement
       1280px. La grille OD/OG fait-elle 416px sans que la quatrième colonne passe à la
       ligne ? Le panneau de 320px est-il à droite et non en dessous ?
       *Si quelque chose se replie, c'est un défaut, pas une préférence.*

    2. **Un avertissement et un refus, côte à côte, sont distinguables EN NIVEAUX DE GRIS.**
       Provoquer les deux en même temps : une addition OD différente de OG (avertissement)
       et un cylindre positif en notation négative (refus). Mettre l'écran en niveaux de
       gris (macOS : Réglages → Accessibilité → Affichage → Filtres de couleur → Niveaux de
       gris). **Sait-on lequel bloque l'enregistrement, sans la couleur ?**
       *C'est la question centrale de la conception de l'avertissement : `03` §10 interdit
       la couleur comme seul porteur de sens, et un avertissement qui ressemble à une
       erreur apprend à l'opticien à ignorer les deux.*

    3. **L'anneau de focus se voit sur chaque champ de la grille, y compris l'axe et son
       suffixe `°`.** Tabuler de la bascule de notation jusqu'au bouton d'enregistrement,
       sans souris. L'anneau est-il visible partout ? Passe-t-il dans l'ordre du papier —
       OD sphère, cylindre, axe, addition, puis OG ? Le `°` ne masque-t-il pas l'anneau de
       l'entrée d'axe ?

    4. **Une ordonnance imprimée est lisible et porte `Cylindre négatif.`** Depuis
       l'historique, `Imprimer`, puis l'aperçu d'impression du navigateur. Les valeurs
       sont-elles lisibles en noir sur blanc ? Le pied porte-t-il la convention ? **La
       photo est-elle bien absente ?**

    5. **Au lecteur d'écran, un avertissement est annoncé poliment une fois, et le champ
       n'est pas annoncé comme invalide.** NVDA ou VoiceOver sur le formulaire de saisie.
       Saisir une addition différente entre les deux yeux, puis quitter le champ.
       L'annonce vient-elle **une** fois, sans interrompre ? Revenir sur le champ : la
       remarque est-elle relue ? Le champ est-il annoncé **invalide** — il ne doit pas
       l'être.

    **Répondre étape par étape** : `1 approuvée`, `2 rejetée : <ce qui a été vu>`, etc.
    Une réponse d'un seul tenant sera reportée telle quelle sur les cinq lignes et notée
    comme telle, mais elle ne permet pas de savoir laquelle a été réellement regardée.

    **Ne corriger aucun défaut pendant la passe.** Les défauts se notent et se corrigent
    après, pour que les nombres relevés avant et après la passe soient comparables.
  </how-to-verify>
  <action>
Préparer la pile, **puis** relever les portes du §1 de `04-VALIDATION.md` AVANT d'ouvrir la
passe : `docker compose up -d`, `uv run python manage.py runserver`,
`npm --prefix web run dev`, puis les sept commandes de la table.

Éprouver les identifiants de développement par un vrai `POST /api/auth/connexion/` — les
mots de passe ont dérivé **trois fois** en phase 3, et une passe qui démarre sur un 400
perd sa première demi-heure. Réparer une dérive avant la passe, la noter dans le SUMMARY.

Présenter les cinq étapes au propriétaire, dans l'ordre, et **attendre un verdict étape par
étape**. Ne corriger aucun défaut pendant la passe.

Après le verdict, **rejouer** les sept portes et les quatorze gardes, et renseigner
`04-VALIDATION.md` avec les deux relevés et leur heure. Reporter le verdict mot pour mot ;
s'il arrive d'un seul tenant, l'écrire ainsi plutôt que de le répartir en le prêtant.

Commit (chemins nommés, aucune mention de Claude) :
`git add .planning/phases/04-clients-ordonnances/04-VALIDATION.md .planning/phases/04-clients-ordonnances/04-09-SUMMARY.md && git commit -m "docs(04-09): la porte de la phase 4, relevee avant et apres la passe"`
  </action>
  <resume-signal>Répondre étape par étape : `1 approuvée` … `5 rejetée : …`</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| charge utile serveur → carte de version | une revalidation à l'affichage ferait paraître fausses les anciennes fiches |
| interface → immuabilité | un contrôle de modification rouvrirait le chemin que CLIENT-06 ferme |
| octets de la photo → `<img src>` | une URL directe contournerait `ordonnance.voir` |
| nom de fichier → DOM, toast, Sentry | il porte couramment le nom du patient |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-63 | Integrity (lisibilité) | version ancienne affichée comme fautive après un changement de bornes | mitigate | `CarteVersion` n'importe **pas** `verifier.ts` — vérifié par grep, pas par relecture. Test dédié sur une valeur hors bornes servies rendue sans note ni badge. |
| T-04-64 | Tampering | contrôle de modification ou de suppression réintroduit | mitigate | Aucun menu de ligne, aucune icône ; grep sur `Supprimer|Modifier|Éditer` avec **contrôle positif** sur `Saisir une ordonnance`. Le serveur rend 405 de toute façon (plan 04-05), donc c'est une défense en profondeur, pas la garantie. |
| T-04-65 | Elevation of privilege | photo atteignable sans `ordonnance.voir` | mitigate | `<img src>` est un chemin d'API derrière la vue du plan 04-06 ; aucun `MEDIA_URL`, aucune URL pré-signée. L'échec rend le message global, jamais une icône cassée qui inviterait à chercher l'URL directe. |
| T-04-66 | Information disclosure | nom de fichier dans une erreur, un toast ou un titre | mitigate | Le nom ne paraît qu'à côté de la vignette, tronqué, dans `<bdi>`, pour un lecteur qui voit déjà le dossier. Test posant `ordonnance_benali_ahmed.jpg` et cherchant `benali` dans le DOM d'erreur. |
| T-04-67 | Tampering | photo remplacée depuis l'interface | mitigate | Aucun contrôle d'attache sur une version qui en porte déjà une, et la phrase explicative à la place. Contrôle positif sur une version sans photo. |
| T-04-68 | Information disclosure | onglet `Achats` révélant une capacité inexistante | accept | Non rendu. Ce n'est pas une menace de sécurité mais une promesse fausse ; l'écrire ici parce que la tentation de « juste mettre l'onglet » reviendra. |
| T-04-69 | Accessibilité | anneau de focus masqué par le suffixe `°` | **accept — vérification manuelle n° 3** | jsdom ne calcule aucun CSS. Aucun test ne peut le juger, et c'est dit plutôt que couvert par une assertion de classe qui aurait l'air de le prouver. |
| T-04-70 | Accessibilité | avertissement lu comme une erreur | mitigate **+ vérification manuelle n° 2** | Les attributs sont testés (plan 04-08) ; la **lisibilité relative** est jugée en niveaux de gris par une personne. Les deux moitiés sont nécessaires et ni l'une ni l'autre ne suffit. |
</threat_model>

<verification>
- `npm --prefix web test` vert ; relever le décompte observé, tests écrits contre
  emplacements neufs.
- `npm --prefix web run build` sort à 0.
- `audit:format`, `audit:projection`, `audit:clinique` sortent tous à 0.
- `uv run pytest -q -m "not slow"` vert — ce plan ne touche pas le serveur, donc le nombre
  ne doit **pas** bouger ; s'il bouge, quelque chose a été committé par erreur dans la
  vague.
- `git diff --quiet <base de la phase> -- web/src/etats/messages.ts` : inchangé. Aucun
  écran de la phase 4 n'a écrit sa propre copie d'un état global.
- `git diff --stat web/src/print.css` : un bloc ajouté, **aucune règle de géométrie de
  page** — le vérifier à l'œil sur le diff.
- Les quatre portes de `04-VALIDATION.md` sont relevées **avant** la passe humaine et
  **rejouées après**, comme en phase 03.1 : une passe au navigateur se fait sur une pile
  qui tourne, et un document qui n'a mesuré qu'un côté ne prouve rien du second.
</verification>

<success_criteria>
1. On atteint l'historique par quatre clics depuis la racine.
2. Une version stockée se rend telle quelle : aucune note, aucun badge d'erreur, aucune
   annotation, même hors des bornes d'aujourd'hui.
3. Aucun contrôle de modification ni de suppression n'existe, avec contrôle positif.
4. Rien n'est rouge sur une version remplacée.
5. L'onglet `Achats` n'est pas rendu, CLIENT-02 n'est pas cochée et **n'est pas déplacée**
   dans `REQUIREMENTS.md`.
6. La photo s'attache une fois ; le contrôle disparaît ensuite et la phrase le remplace.
7. La feuille imprimée porte `Cylindre négatif.` et aucune photo, sans géométrie ajoutée.
8. La passe humaine compte exactement **cinq** étapes, et le solde de la phase 3 n'en
   reçoit aucune de plus.
</success_criteria>

<output>
Au terme du plan, écrire `.planning/phases/04-clients-ordonnances/04-09-SUMMARY.md` et
renseigner `.planning/phases/04-clients-ordonnances/04-VALIDATION.md`.

Y consigner : le décompte web avant et après, **tests écrits contre emplacements neufs** ;
les quatre portes mécaniques relevées **avant** la passe **et rejouées après**, avec l'heure
des deux relevés ; le verdict du propriétaire **étape par étape**, mot pour mot, et
explicitement noté comme global s'il répond d'un seul tenant ; et tout défaut relevé
pendant la passe, **non corrigé** pendant celle-ci.

Rappel : ce plan est seul dans sa vague, donc pas de contrainte d'index partagé. Continuer
néanmoins à **nommer les chemins à chaque commit** — c'est l'habitude qui a manqué une fois.
</output>
</content>
</invoke>
