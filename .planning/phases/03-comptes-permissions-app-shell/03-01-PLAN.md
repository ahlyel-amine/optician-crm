---
phase: 03-comptes-permissions-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - config/settings/base.py
  - config/urls.py
  - plateforme/tenancy/router.py
  - plateforme/tenancy/middleware.py
  - plateforme/comptes/__init__.py
  - plateforme/comptes/apps.py
  - plateforme/comptes/models.py
  - plateforme/comptes/managers.py
  - plateforme/comptes/admin.py
  - plateforme/comptes/migrations/__init__.py
  - plateforme/comptes/migrations/0001_initial.py
  - plateforme/projection/__init__.py
  - plateforme/projection/apps.py
  - plateforme/control_plane/admin.py
  - docker/postgres/init/00-databases.sql
  - tests/fixtures/formats_mad.json
  - tests/test_comptes_socle.py
autonomous: true
requirements: [PERM-02]
user_setup: []

must_haves:
  truths:
    - "manage.py check passes with comptes, projection and drf_spectacular installed and classified"
    - "AUTH_USER_MODEL resolves to comptes.Utilisateur and the control-plane database carries its table"
    - "A user row bound to a client can never be is_staff or is_superuser, enforced by the database"
    - "A client PostgreSQL role cannot open the control-plane database"
    - "One shared MAD/date format fixture exists and is readable from both tests/ and web/"
  artifacts:
    - path: "plateforme/comptes/models.py"
      provides: "Utilisateur (AbstractBaseUser, no PermissionsMixin) with three constraints"
      contains: "un_utilisateur_client_n_est_jamais_operateur"
    - path: "plateforme/comptes/migrations/0001_initial.py"
      provides: "the control-plane user table"
    - path: "tests/fixtures/formats_mad.json"
      provides: "the single money/date format specification read by pytest and vitest"
      contains: "MAD"
    - path: "docker/postgres/init/00-databases.sql"
      provides: "REVOKE CONNECT on the control-plane database (CLAUDE.md #12)"
      contains: "REVOKE CONNECT ON DATABASE optique_control FROM PUBLIC"
  key_links:
    - from: "config/settings/base.py"
      to: "plateforme.comptes.models.Utilisateur"
      via: "AUTH_USER_MODEL"
      pattern: "AUTH_USER_MODEL"
    - from: "plateforme/tenancy/middleware.py"
      to: "Utilisateur.client_id"
      via: "resolve_client reads user.client_id - unchanged, now satisfied by a real user"
      pattern: "client_id"
    - from: "plateforme/tenancy/router.py"
      to: "CONTROL_PLANE_APPS"
      via: "comptes, projection and drf_spectacular classified in the same change as INSTALLED_APPS"
      pattern: "comptes"
---

<objective>
Pose le socle de la phase : les dependances, les deux nouvelles applications de plan de
controle, le modele `Utilisateur`, la bascule de `AUTH_USER_MODEL`, le durcissement de
l'admin operateur, la fermeture du trou `CONNECT` sur la base du plan de controle, et le
fichier de format partage que pytest et vitest liront tous les deux.

Purpose: rien d'autre dans la phase ne peut demarrer tant que `AUTH_USER_MODEL` n'a pas
bascule, et la bascule doit avoir lieu avant que quoi que ce soit ne depende de
`auth.User`. C'est egalement ici que se ferme un trou CLAUDE.md #12 preexistant :
`optique_control` n'a jamais recu son `REVOKE CONNECT ... FROM PUBLIC`, et cette phase y
depose les empreintes de mots de passe et les droits de toute la flotte.

Output: `plateforme/comptes/` avec `Utilisateur`, `plateforme/projection/` (coquille
etiquetee), `tests/fixtures/formats_mad.json`, un `manage.py check` propre.

**Cette phase suit `03-UI-SPEC.md` 0.1 et CLAUDE.md #13, pas `03-RESEARCH.md` section 4.**
Le bandeau de correction en tete de la recherche le dit explicitement. La forme des droits
est posee au plan 03-04 ; ici on ne cree que `Utilisateur`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/TESTING.md
@.planning/phases/03-comptes-permissions-app-shell/03-RESEARCH.md
@.planning/phases/03-comptes-permissions-app-shell/03-VALIDATION.md
@config/settings/base.py
@plateforme/tenancy/router.py
@plateforme/tenancy/middleware.py
@plateforme/control_plane/models.py
@docker/postgres/init/00-databases.sql
@tests/test_pgbouncer_auth.py
</context>

<interfaces>
Ce que le plan consomme, extrait du depot. L'executant n'a pas a explorer.

`plateforme/tenancy/middleware.py` (inchange par ce plan, sauf les docstrings) :

    def resolve_client(request):
        user = getattr(request, "user", None)
        if user is None or not getattr(user, "is_authenticated", False):
            return None
        client_id = getattr(user, "client_id", None)   # le champ que Utilisateur doit porter
        if client_id is None:
            return None
        return Client.objects.using("default").filter(pk=client_id, status=Client.ACTIVE).first()

`plateforme/tenancy/router.py` :

    CONTROL_PLANE_APPS = frozenset({"control_plane", "admin", "auth", "contenttypes",
                                    "sessions", "messages", "django_celery_beat",
                                    "rest_framework", "tenancy"})
    BUSINESS_APPS = frozenset({"magasins", "stock", "caisse"})

`plateforme/tenancy/checks.py` n'exempte que les noms commencant par `django.` : toute
application ajoutee a `INSTALLED_APPS` doit entrer dans l'un des deux ensembles **dans le
meme changement**, sinon `tenancy.E001` casse `manage.py check`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-vol, dependances, applications classees, fixture de format partagee</name>
  <read_first>
    - `pyproject.toml` : la discipline d'epinglage exact et le bloc `[tool.uv]`
    - `plateforme/tenancy/router.py` : `CONTROL_PLANE_APPS`, et pourquoi omettre une entree casse `manage.py check`
    - `plateforme/tenancy/checks.py` : `tenancy.E001`, l'exemption `django.*`
    - `config/settings/base.py` : bloc `INSTALLED_APPS`
    - `03-RESEARCH.md` section "Standard Stack" (versions verifiees le 2026-09-14) et Pitfall P1
    - `03-UI-SPEC.md` section 8.1 : le format cible et la liste de cas
  </read_first>
  <files>pyproject.toml, uv.lock, config/settings/base.py, plateforme/tenancy/router.py, plateforme/comptes/__init__.py, plateforme/comptes/apps.py, plateforme/projection/__init__.py, plateforme/projection/apps.py, plateforme/comptes/migrations/__init__.py, tests/fixtures/formats_mad.json</files>
  <action>
**1. Pre-vol bloquant, avant tout autre geste.** Compter les clients ACTIVE du plan de
controle avec `manage.py shell -c` sur
`Client.objects.using("default").filter(status=Client.ACTIVE).count()`.

Si le resultat n'est **pas zero**, **arreter ce plan immediatement** et le signaler : la
bascule de `AUTH_USER_MODEL` devient alors une migration de donnees multi-etapes qui a
besoin de son propre plan (`03-RESEARCH.md` P7). Ne pas improviser la migration.
Si le resultat est zero, continuer.

**2. Dependances, epinglees exactement** (versions verifiees 2026-09-14 dans
`03-RESEARCH.md` section "Standard Stack") :

    uv add drf-spectacular==0.30.0 argon2-cffi==25.1.0

Ne pas ajouter `djangorestframework-simplejwt` : il ne supporte pas Django 6.1, et
l'authentification de cette phase est par session (table de pile CLAUDE.md, corrigee et
verifiee). `django-axes` est **hors perimetre** : le plan 03-08 couvre le bourrage
d'identifiants avec `ScopedRateThrottle`, qui ne demande aucune dependance.

Prouver que `drf-spectacular` tourne sur Django 6.1.1 (assomption A6 de la recherche, ses
classifiers s'arretent a 6.0) : ajouter `drf_spectacular` a `INSTALLED_APPS` et verifier
que `manage.py check` passe. Si l'import casse, s'arreter et le signaler : rien dans
PERM-06 ne fonctionne sans lui.

**3. Les deux applications, creees et classees dans le meme changement.**

- `plateforme/comptes/` : `__init__.py`, `apps.py` (`ComptesConfig`, `name =
  "plateforme.comptes"`, `label = "comptes"`), `migrations/__init__.py`.
- `plateforme/projection/` : `__init__.py`, `apps.py` (`ProjectionConfig`, `label =
  "projection"`). Elle **ne possede aucun modele** mais a quand meme besoin de son
  etiquette, sinon `tenancy.E001` la refuse.
- `config/settings/base.py` : ajouter `"drf_spectacular"`, `"plateforme.comptes"`,
  `"plateforme.projection"` a `INSTALLED_APPS`.
- `plateforme/tenancy/router.py` : ajouter `"comptes"`, `"projection"`,
  `"drf_spectacular"` a `CONTROL_PLANE_APPS`, **dans le meme commit**, avec un commentaire
  sur place dans l'idiome du fichier.

**4. La fixture de format partagee**, `tests/fixtures/formats_mad.json`, ecrite avant les
deux implementations qui la liront (serveur au plan 03-10, client au plan 03-11). Sa forme,
avec les separateurs ecrits en echappement JSON ` ` :

    {
      "separateur_decimal": ",",
      "separateur_milliers": " ",
      "separateur_avant_devise": " ",
      "decimales": 2,
      "devise": "MAD",
      "arrondi": "ROUND_HALF_UP",
      "format_date_court": "dd/MM/yyyy",
      "fuseau_affichage": "Africa/Casablanca",
      "cas_montants": [
        ["0.00",       "0,00 MAD"],
        ["0.05",       "0,05 MAD"],
        ["1800.00",    "1 800,00 MAD"],
        ["1234567.89", "1 234 567,89 MAD"],
        ["-12.30",     "-12,30 MAD"],
        ["999.995",    "1 000,00 MAD"]
      ],
      "cas_dates": [
        ["2026-09-14T09:14:00Z", "14/09/2026", "14/09/2026 à 10:14"]
      ]
    }

Les separateurs sont en echappement **deliberement** : un U+00A0 litteral dans un fichier
source est invisible en revue et un editeur peut le normaliser en espace ordinaire.
CLAUDE.md #14 et `03-UI-SPEC.md` 8.1 fixent U+00A0, jamais U+202F, jamais le point de
`fr-MA`. Le cas `999.995` epingle l'arrondi (`ROUND_HALF_UP` sur un `Decimal`, jamais un
flottant). Le cas de date porte la conversion UTC vers Africa/Casablanca (UTC+1) :
`TIME_ZONE = "UTC"` reste porteur pour PgBouncer et **ne doit pas** devenir
`Africa/Casablanca` (`03-UI-SPEC.md` 8.2).
  </action>
  <acceptance_criteria>
    - `uv run python manage.py check` sort en code 0
    - `grep -c 'drf-spectacular==0.30.0' pyproject.toml` retourne 1, idem pour `argon2-cffi==25.1.0`
    - `grep -ci simplejwt pyproject.toml` retourne 0
    - `grep -cE '"comptes"|"projection"|"drf_spectacular"' plateforme/tenancy/router.py` retourne 3
    - `test -f tests/fixtures/formats_mad.json`
    - un `python -c` qui charge le JSON et affirme `d["separateur_milliers"] == " "` et que le couple `["1800.00", "1 800,00 MAD"]` est dans `cas_montants` sort en code 0
    - `grep -c 'fr-MA' tests/fixtures/formats_mad.json` retourne 0
  </acceptance_criteria>
  <verify>
    <automated>uv run python manage.py check</automated>
  </verify>
  <done>`manage.py check` est propre avec les cinq applications installees et classees, et la fixture de format existe avec U+00A0 epingle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Utilisateur sur le plan de controle, bascule de AUTH_USER_MODEL, Argon2</name>
  <read_first>
    - `plateforme/control_plane/models.py` : l'idiome `CheckConstraint` +
      `violation_error_message` deja utilise par `active_client_has_db_name`
    - `plateforme/tenancy/middleware.py` lignes 36-67 : `resolve_client` lit
      `user.client_id` ; nommer le champ `client` est ce qui fait que la couche tenancy ne
      change pas d'une ligne
    - `03-RESEARCH.md` section 1 (forme du modele, les trois contraintes, pourquoi pas de
      `PermissionsMixin`) et P7 (le danger de la bascule)
    - `03-UI-SPEC.md` 0.4 : l'identifiant est une adresse e-mail, parce que l'unicite est a
      l'echelle de la flotte
    - `.planning/TESTING.md` section 1 : ne pas tester Django, tester nos regles
  </read_first>
  <files>plateforme/comptes/models.py, plateforme/comptes/managers.py, plateforme/comptes/migrations/0001_initial.py, config/settings/base.py, tests/test_comptes_socle.py</files>
  <behavior>
    - `test_perm02_un_utilisateur_client_ne_peut_jamais_etre_operateur` : creer un
      utilisateur rattache a un client, puis
      `Utilisateur.objects.filter(pk=...).update(is_staff=True)` doit lever
      `IntegrityError`. Passer par `queryset.update()` **est** le test : il prouve que la
      garantie est une contrainte de base et non un `clean()` que le shell contourne.
      Repeter avec `is_superuser=True`.
    - `test_perm02_un_seul_proprietaire_par_client` : deux `est_proprietaire=True` sur le
      meme client leve `IntegrityError`.
    - `test_perm02_un_proprietaire_appartient_a_un_client` : `est_proprietaire=True` avec
      `client=None` leve `IntegrityError`.
    - `test_perm02_utilisateur_n_expose_aucun_systeme_de_droits_django` :
      `hasattr(u, "groups")` est faux et `hasattr(u, "user_permissions")` est faux. Deux
      systemes de permissions dans un meme code, c'est la derive de la phase 9 ; et `Group`
      **est** un palier de role, interdit par CLAUDE.md #6.
  </behavior>
  <action>
Ecrire d'abord les quatre tests ci-dessus dans `tests/test_comptes_socle.py`, les voir
rouges, puis implementer.

`plateforme/comptes/managers.py` : `UtilisateurManager(BaseUserManager)` avec
`create_user(email, nom_complet, password=None, **extra)` et `create_superuser(...)` qui
force `client=None`, `is_staff=True`, `is_superuser=True` (un operateur n'appartient a
aucun client : la contrainte l'exige). Normaliser l'e-mail entierement en minuscules :
l'unicite est a l'echelle de la flotte et deux opticiens ne doivent pas pouvoir creer
`Karim@...` et `karim@...`.

`plateforme/comptes/models.py` : `Utilisateur(AbstractBaseUser)`, **sans
`PermissionsMixin`**, exactement la forme de `03-RESEARCH.md` section 1 :

- `email` (unique, `verbose_name="adresse e-mail"`), `nom_complet`
- `client = FK("control_plane.Client", null=True, blank=True, on_delete=PROTECT,
  related_name="utilisateurs")`. **Le nom du champ est `client`**, parce que
  `resolve_client` lit `user.client_id`.
- `est_proprietaire`, `is_active`, `is_staff`, `is_superuser`,
  `doit_changer_mot_de_passe`, `derniere_connexion_ip`, `cree_le`
- `USERNAME_FIELD = "email"`, `REQUIRED_FIELDS = ["nom_complet"]`
- les trois contraintes : `un_utilisateur_client_n_est_jamais_operateur`,
  `un_proprietaire_appartient_a_un_client`, `un_seul_proprietaire_par_client`
- `has_perm` / `has_module_perms` renvoyant `self.is_superuser` (l'admin en a besoin)
- libelles en francais, comme le reste du depot

`config/settings/base.py` : `AUTH_USER_MODEL = "comptes.Utilisateur"` et
`PASSWORD_HASHERS` avec `Argon2PasswordHasher` en tete puis `PBKDF2PasswordHasher`.
Django fournit le backend Argon2 ; `argon2-cffi` est la bibliotheque C manquante.

Generer la migration (`manage.py makemigrations comptes`), puis **recreer la base de
developpement du plan de controle** : la bascule ne peut pas s'appliquer par-dessus les
migrations `auth`/`admin` existantes (P7). Sequence : `DROP DATABASE ... WITH (FORCE)`,
rejouer `docker/postgres/init/00-databases.sql`, puis `manage.py migrate`.

Le superutilisateur operateur cree pendant la phase 2 est detruit par la bascule : le
recreer avec `manage.py createsuperuser` et le noter dans le SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -c 'AUTH_USER_MODEL = "comptes.Utilisateur"' config/settings/base.py` retourne 1
    - `grep -c Argon2PasswordHasher config/settings/base.py` retourne 1
    - `grep -c PermissionsMixin plateforme/comptes/models.py` retourne 0
    - `grep -c un_utilisateur_client_n_est_jamais_operateur plateforme/comptes/models.py` retourne 1
    - `test -f plateforme/comptes/migrations/0001_initial.py`
    - `uv run pytest tests/test_comptes_socle.py -x -q -k perm02` : 4 tests passent
    - `uv run python manage.py makemigrations --check --dry-run` sort en code 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_comptes_socle.py -x -q -k perm02</automated>
  </verify>
  <done>`AUTH_USER_MODEL` pointe sur `comptes.Utilisateur`, la table existe sur `default`, et les quatre tests nommes PERM-02 passent, dont celui qui prouve par `queryset.update()` que la garantie operateur est une contrainte de base.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Admin operateur verrouille et REVOKE CONNECT sur le plan de controle</name>
  <read_first>
    - `config/urls.py` : sa docstring affirme aujourd'hui quelque chose de **faux**, la
      resolution de tenant n'est pas fonction du chemin (`03-RESEARCH.md` correction 2)
    - `plateforme/control_plane/admin.py` : `ClientAdmin` expose `db_name`, `db_host`,
      `db_password_encrypted`
    - `docker/postgres/init/00-databases.sql` : `optique_control` n'a jamais recu son
      `REVOKE CONNECT ... FROM PUBLIC`
    - `docker/postgres/init/01-pgbouncer-auth.sql` lignes 84-110 : l'idiome exact du REVOKE
      et son commentaire, a imiter
    - `tests/test_pgbouncer_auth.py` lignes 181-240 : la forme du test "se connecter comme
      le mauvais principal et etre refuse"
    - `plateforme/tenancy/provisioner.py` lignes 190-210 : `REVOKE CONNECT` puis
      `GRANT CONNECT` au seul role proprietaire
    - CLAUDE.md #12
  </read_first>
  <files>plateforme/comptes/admin.py, plateforme/control_plane/admin.py, config/urls.py, plateforme/tenancy/middleware.py, docker/postgres/init/00-databases.sql, tests/test_comptes_socle.py</files>
  <behavior>
    - `test_perm06_aucun_modele_metier_n_est_enregistre_dans_ladmin` : parcourir
      `admin.site._registry` et affirmer qu'aucun modele dont `_meta.app_label` est dans
      `BUSINESS_APPS` n'y figure. Un `ModelAdmin` metier tourne sans projection.
    - `test_perm02_seul_un_operateur_sans_client_atteint_ladmin` : `has_permission` est
      faux pour un utilisateur `is_superuser=True` porteur d'un `client_id` (cas
      inatteignable en base grace a la contrainte, donc construit en memoire), et vrai pour
      un operateur dont `client_id` est `None`.
    - `test_perm02_un_role_client_ne_peut_pas_ouvrir_la_base_du_plan_de_controle`, marque
      `slow` : creer un role `test_client_uXXXXXX`, tenter une connexion psycopg directe a
      la base du plan de controle avec ce role, affirmer le refus. Sans le `REVOKE`,
      PostgreSQL accorde `CONNECT` a `PUBLIC` sur toute base neuve, et cette base porte
      desormais les empreintes de mots de passe et les droits de **toute la flotte**.
      Nettoyer le role dans un `finally`.
  </behavior>
  <action>
**1. `AdminSite` operateur.** Dans `plateforme/comptes/admin.py`, definir un `AdminSite`
dont `has_permission(request)` exige `request.user.is_active`,
`request.user.is_superuser` et `request.user.client_id is None`, et l'installer comme
`admin.site`. La contrainte de base du Task 2 est la defense de fond ; ceci est la seconde.

Enregistrer `Utilisateur` cote operateur en lecture seule sur les champs sensibles : liste
et recherche par e-mail, aucun champ `client` ni `est_proprietaire` modifiable.
**N'enregistrer aucun modele metier**, jamais.

**2. Corriger la docstring mensongere de `config/urls.py`.** Elle affirme que
`resolve_client` renvoie deliberement `None` pour ce chemin. C'est faux : la resolution se
fait par `client_id`, pas par chemin, et a partir de cette phase il existe des
utilisateurs porteurs d'un `client_id`. Ecrire la verite : l'admin est reserve aux
operateurs, garanti par la contrainte de base et `has_permission`, jamais par l'URL.

**3. Corriger les deux docstrings tenancy perimees** dans
`plateforme/tenancy/middleware.py` : le module et `resolve_client` annoncent tous deux que
la phase 3 emet des JWT portant une revendication `client_id`. La phase 3 utilise
**l'authentification par session**. Sous JWT rien ne se lie, parce que DRF authentifie dans
`APIView.initial()`, apres tous les middlewares, et toute requete metier leverait
`NoTenantBound`. Aucune ligne de code ne change dans ce fichier, seulement les docstrings.

**4. Fermer le trou `CONNECT` (CLAUDE.md #12).** Dans
`docker/postgres/init/00-databases.sql`, juste apres le `\gexec` qui cree la base du plan
de controle, ajouter, avec un commentaire expliquant que PostgreSQL accorde `CONNECT` a
`PUBLIC` sur toute base neuve et que cette base porte les empreintes de mots de passe et
les droits de toute la flotte :

    REVOKE CONNECT ON DATABASE optique_control FROM PUBLIC;
    GRANT  CONNECT ON DATABASE optique_control TO optique_app;

Le script est idempotent et rejouable a la main : appliquer les deux instructions a la
grappe locale dans ce meme task, et le noter dans le SUMMARY, car les grappes de
developpement deja initialisees ne rejouent pas `docker-entrypoint-initdb.d`.
  </action>
  <acceptance_criteria>
    - `grep -c 'REVOKE CONNECT ON DATABASE optique_control FROM PUBLIC' docker/postgres/init/00-databases.sql` retourne 1
    - `grep -cE 'GRANT +CONNECT ON DATABASE optique_control TO optique_app' docker/postgres/init/00-databases.sql` retourne 1
    - `grep -ci jwt plateforme/tenancy/middleware.py` retourne 0
    - `grep -ci 'deliberately returns' config/urls.py` retourne 0
    - `uv run pytest tests/test_comptes_socle.py -q -k "ladmin or operateur"` passe
    - `uv run pytest tests/test_comptes_socle.py -q -k plan_de_controle -m slow` passe avec Compose demarre
    - `psql -c "\l optique_control"` ne montre plus de privilege `c` pour `PUBLIC`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_comptes_socle.py -x -q</automated>
  </verify>
  <done>L'admin est inatteignable pour un utilisateur rattache a un client, aucun modele metier n'y est enregistre, les docstrings perimees disent la verite, et un role client se voit refuser l'ouverture de la base du plan de controle, prouve par un test qui se connecte comme le mauvais principal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| navigateur vers Django | entree non fiable, aucune identite tant que la session n'est pas etablie |
| utilisateur client vers admin Django | franchissement d'elevation, l'admin est la gestion de flotte |
| role PostgreSQL client vers la base du plan de controle | franchissement inter-locataires **sous** Python, le seul de cette phase qui possede une couche sous l'application |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Elevation | `Utilisateur.is_staff` sur un compte rattache a un client (A-03-01) | mitigate | `CheckConstraint un_utilisateur_client_n_est_jamais_operateur`, prouve par `queryset.update()`, plus `AdminSite.has_permission` exigeant `is_superuser` et `client_id is None` (Tasks 2 et 3) |
| T-03-02 | Info disclosure | `ClientAdmin` expose `db_name`, `db_host` et `db_password_encrypted` de chaque opticien | mitigate | meme fermeture que T-03-01 : seul un operateur sans client atteint l'admin (Task 3) |
| T-03-03 | Info disclosure | tout role client peut ouvrir la base du plan de controle, `CONNECT` etant accorde a `PUBLIC` par defaut (CLAUDE.md #12) | mitigate | `REVOKE CONNECT ... FROM PUBLIC` puis `GRANT` au seul `optique_app`, avec un test qui se connecte comme le mauvais principal et est refuse (Task 3) |
| T-03-04 | Elevation | `Group` et `user_permissions` offriraient un second systeme de droits, donc un palier de role interdit par CLAUDE.md #6 | mitigate | pas de `PermissionsMixin` : l'absence est la garantie, et elle est testee (Task 2) |
| T-03-05 | Spoofing | un indice de locataire fourni par l'appelant (en-tete, sous-domaine, parametre) | accept | inchange depuis la phase 2 : `resolve_client` ne lit que le principal authentifie. Ce plan ne touche aucune ligne de code de `middleware.py` |
| T-03-06 | Tampering | bascule de `AUTH_USER_MODEL` par-dessus des donnees reelles | mitigate | pre-vol SQL bloquant en Task 1 ; si des clients ACTIVE existent, le plan s'arrete et une migration de donnees prend le relais |

**Admission honnete, repetee dans chaque plan de cette phase.** Contrairement a l'isolation
inter-clients, les permissions par gerant **n'ont aucune couche d'execution sous Python** :
tous les utilisateurs d'un client partagent le role PostgreSQL `optique_uNNNNNN`. Le
controle compensatoire est l'enumerabilite (registre plus test parametre, plans 03-06 et
03-07), pas la profondeur. Les roles PostgreSQL par gerant sont rejetes sur une base
mesuree : les pools PgBouncer sont cles par `(user, database)`, donc N gerants
multiplieraient par N le budget de connexions mesure en TENANT-08.
</threat_model>

<verification>
- `uv run python manage.py check` : 0 issue, ni `tenancy.E001` ni erreur d'import `drf_spectacular`
- `uv run python manage.py makemigrations --check --dry-run` : code 0
- `uv run pytest -x -q -m "not slow and not pending"` : la suite existante (107 tests) reste verte
- `uv run pytest tests/test_comptes_socle.py -q` : les tests nommes PERM-02 et PERM-06 de ce plan passent
- l'ACL de la base du plan de controle n'accorde plus `CONNECT` a `PUBLIC`
</verification>

<success_criteria>
- `AUTH_USER_MODEL = "comptes.Utilisateur"`, table presente sur `default` uniquement
- `comptes`, `projection` et `drf_spectacular` installes **et** classes dans `CONTROL_PLANE_APPS`
- Un compte rattache a un client ne peut pas devenir operateur, garanti en base
- La base du plan de controle refuse `CONNECT` a `PUBLIC`, prouve par un test
- `tests/fixtures/formats_mad.json` existe, avec U+00A0, et ne mentionne pas `fr-MA`
- Aucune docstring du depot n'annonce plus des JWT pour la phase 3
</success_criteria>

<output>
Apres execution, creer `.planning/phases/03-comptes-permissions-app-shell/03-01-SUMMARY.md`.
Y consigner : le resultat du pre-vol SQL, le fait que le superutilisateur operateur a du
etre recree, et que `00-databases.sql` doit etre rejoue a la main sur toute grappe de
developpement deja initialisee.
</output>
