---
phase: 03-comptes-permissions-app-shell
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - conftest.py
  - tests/factories.py
  - tests/test_comptes_auth.py
  - tests/test_comptes_droits.py
  - tests/test_projection.py
  - tests/test_magasin_acces.py
  - tests/test_locale.py
  - tests/test_schema_contrat.py
autonomous: true
requirements: [PERM-01, PERM-02, PERM-03, PERM-04, PERM-05, PERM-06, APP-01, APP-03]
user_setup: []

must_haves:
  truths:
    - "Every named test in 03-VALIDATION.md exists as a collected pytest function"
    - "No named test is silently absent; unimplemented ones are marked pending, not skipped by convention"
    - "Test fixtures provide two magasins per tenant, as they already provide two tenants"
    - "A gerant factory, an owner factory and grant factories exist and write to the right database"
  artifacts:
    - path: "tests/test_projection.py"
      provides: "the parametrized PERM-06 conformance test scaffold over the registry x renderers"
      contains: "test_perm06_champ_protege_absent_de_lapi_de_lexport_et_du_document"
    - path: "tests/factories.py"
      provides: "ProprietaireFactory, GerantFactory, DroitAccordeFactory, AccesMagasinFactory"
      contains: "GerantFactory"
    - path: "conftest.py"
      provides: "two magasins per tenant fixture"
      contains: "deux_magasins"
  key_links:
    - from: "tests/test_*.py"
      to: ".planning/phases/03-comptes-permissions-app-shell/03-VALIDATION.md"
      via: "one collected function per named test in the per-requirement verification map"
      pattern: "test_perm0|test_app0"
    - from: "tests/factories.py"
      to: "plateforme.comptes.models.Utilisateur"
      via: "DjangoModelFactory with database = default"
      pattern: "database = \"default\""
---

<objective>
Ecrire les tests avant le code. Ce plan cree **toutes** les fonctions de test nommees du
`03-VALIDATION.md`, les factories de comptes et de droits, et le doublement des magasins
dans les fixtures, afin que chaque plan d'implementation qui suit commence par un rouge
nomme et non par une page blanche.

Purpose: ce projet est test-first et `.planning/TESTING.md` exige que chaque exigence soit
prouvee par un test qui la cite. Si les tests naissent apres le code, ils sont ecrits pour
passer. Ici, ils naissent avant et portent le marqueur `pending`.

Output: six fichiers de test peuples de stubs nommes, `tests/factories.py` etendu, et une
fixture `deux_magasins` dans `conftest.py`.

**Ce plan n'implemente aucune exigence** ; il en ecrit les tests nommes. Son champ
`requirements` liste les huit exigences parce qu'il cree le test nomme de chacune. La
propriete d'implementation appartient aux plans 03-04 a 03-14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/TESTING.md
@.planning/phases/03-comptes-permissions-app-shell/03-VALIDATION.md
@.planning/phases/03-comptes-permissions-app-shell/03-RESEARCH.md
@conftest.py
@tests/factories.py
@tests/test_magasin_scoping.py
@pyproject.toml
</context>

<interfaces>
Le marqueur `pending` existe deja dans `pyproject.toml` :

    markers = [
        "slow: ...",
        "tenancy: ...",
        "pending: the named test exists but its implementation has not landed yet; the plan
         that implements it removes this marker. Deselected from the quick loop.",
    ]

La boucle rapide du projet est donc :

    uv run pytest -x -q -m "not slow and not pending"

et la suite complete, qui **doit** voir les stubs echouer ou etre deselectionnes selon le
cas, est `uv run pytest --create-db`.

`tests/factories.py` existant (a etendre, pas a reecrire) :

    class ClientFactory(factory.django.DjangoModelFactory):   # ecrit sur `default`, PENDING par defaut
    class MagasinFactory(factory.django.DjangoModelFactory):  # ecrit sur l'alias tenant lie

`conftest.py` fournit deja `db_all`, `tenant_a`, `tenant_b`,
`allow_runtime_tenant_aliases`, et applique `django_db(databases=TENANT_DBS)` a la
collecte. **Ne rien y surcharger de pytest-django** : la docstring du fichier explique
pourquoi c'est interdit.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Factories de comptes et de droits, deux magasins par tenant</name>
  <read_first>
    - `tests/factories.py` : l'asymetrie deliberee entre `ClientFactory` (`database =
      "default"`) et `MagasinFactory` (aucun alias nomme, le routeur decide)
    - `conftest.py` : `TENANT_DBS`, `db_all`, `tenant_a`, `tenant_b`, et l'interdiction
      formelle de surcharger les fixtures de pytest-django
    - `plateforme/comptes/models.py` (cree au plan 03-01) : `Utilisateur` et ses contraintes
    - `03-RESEARCH.md` P16 : tester avec un seul magasin est le meme angle mort que tester
      avec un seul locataire
    - `.planning/TESTING.md` section 3
  </read_first>
  <files>tests/factories.py, conftest.py</files>
  <action>
Etendre `tests/factories.py` en respectant son asymetrie documentee : les comptes et les
droits vivent dans le plan de controle, donc **toutes** ces factories declarent
`database = "default"`.

- `UtilisateurFactory` : e-mail sequentiel, `nom_complet`, `client = None`,
  `est_proprietaire = False`, `is_active = True`. Definir le mot de passe via
  `factory.post_generation` appelant `set_password`, jamais en assignant `password`
  directement (un hash Argon2 n'est pas une chaine en clair).
- `ProprietaireFactory(UtilisateurFactory)` : `est_proprietaire = True`, `client`
  obligatoire. Une seule par client, la contrainte `un_seul_proprietaire_par_client` le
  garantit.
- `GerantFactory(UtilisateurFactory)` : `est_proprietaire = False`, `client` obligatoire.
- `AccesMagasinFactory` et `DroitAccordeFactory` : leurs modeles arrivent au plan 03-04.
  Les ecrire ici **derriere un import differe a l'interieur de `Meta`** n'est pas possible
  avec factory_boy ; les declarer plutot dans un bloc protege documente, ou plus simplement
  les ajouter au plan 03-04. **Choix retenu : les declarer au plan 03-04**, et ne laisser
  ici qu'un commentaire nomme disant ou elles arrivent. Ne pas inventer les modeles.

Dans `conftest.py`, ajouter une fixture `deux_magasins(tenant_a)` qui cree **deux**
`Magasin` actifs (`ANFA` et `MAARIF`) dans le locataire lie et les renvoie. Documenter sur
place, dans l'idiome du fichier, la raison : un bug de portee qui renvoie toujours le meme
magasin passe tous les tests s'il n'y en a qu'un, exactement comme un bug de routeur passe
tous les tests s'il n'y a qu'un locataire (`03-RESEARCH.md` P16). Ajouter la meme fixture
pour `tenant_b` si le test d'isolation croisee en a besoin, ou parametrer la fixture par
alias.

Ne surcharger **aucune** fixture de pytest-django. Ne pas toucher `pytest_sessionstart`
ni `pytest_collection_modifyitems`.
  </action>
  <acceptance_criteria>
    - `grep -cE 'class (ProprietaireFactory|GerantFactory|UtilisateurFactory)' tests/factories.py` retourne 3
    - `grep -c 'database = "default"' tests/factories.py` retourne au moins 2
    - `grep -c 'set_password' tests/factories.py` retourne au moins 1
    - `grep -c 'def deux_magasins' conftest.py` retourne 1
    - `grep -c 'django_db_setup\|django_db_modify_db_settings' conftest.py` retourne 0
    - `uv run pytest -x -q -m "not slow and not pending"` reste vert (107 tests plus ceux du plan 03-01)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -x -q -m "not slow and not pending"</automated>
  </verify>
  <done>Les factories de comptes ecrivent sur `default`, les mots de passe passent par `set_password`, et une fixture fournit deux magasins actifs par locataire.</done>
</task>

<task type="auto">
  <name>Task 2: Les tests nommes de PERM-01 a PERM-04 et APP-01/APP-03, en attente</name>
  <read_first>
    - `03-VALIDATION.md` section "Per-Requirement Verification Map" : la liste exacte des
      noms, qui est le contrat de ce task
    - `03-RESEARCH.md` section "Validation Architecture" : ce que chaque test doit prouver
    - `pyproject.toml` : le marqueur `pending` et sa semantique
    - `tests/test_tenancy_context.py` : le style de docstring de test du depot, une
      docstring qui explique **pourquoi** le test existe, pas ce qu'il fait
  </read_first>
  <files>tests/test_comptes_auth.py, tests/test_comptes_droits.py, tests/test_magasin_acces.py, tests/test_locale.py</files>
  <action>
Creer un stub pour **chaque** nom de `03-VALIDATION.md`, avec :

- le nom exact, mot pour mot ;
- une docstring qui cite son exigence et dit ce que sa rougeur prouverait ;
- `@pytest.mark.pending` ;
- un corps `pytest.fail("non implemente : plan 03-XX")` nommant le plan qui l'implementera ;
- **tous les imports du code en cours de construction a l'interieur de la fonction**, jamais
  au module. C'est la convention etablie en phase 2 : un import de module vers un symbole
  qui n'existe pas encore casse la **collecte**, ce qui rend toute la suite rouge pour une
  mauvaise raison.

`tests/test_comptes_auth.py`, PERM-01, implemente au plan 03-08 :
`test_perm01_la_connexion_etablit_une_session_et_lie_le_client`,
`test_perm01_le_cookie_de_session_survit_a_la_fermeture_du_navigateur`,
`test_perm01_la_deconnexion_invalide_la_session_immediatement`,
`test_perm01_la_connexion_est_limitee_en_debit`,
`test_perm01_une_requete_api_sans_jeton_csrf_est_refusee`.

`tests/test_comptes_droits.py`, PERM-02 et PERM-03, implementes aux plans 03-04, 03-05 et
03-09 : `test_perm02_un_proprietaire_ne_cree_un_compte_que_dans_son_propre_client`,
`test_perm02_un_compte_desactive_est_refuse_des_la_requete_suivante`,
`test_perm03_un_droit_est_une_ligne_pas_un_palier`,
`test_perm03_la_revocation_prend_effet_sans_reconnexion`,
`test_perm03_un_proprietaire_ne_peut_pas_accorder_a_un_utilisateur_dun_autre_client`,
`test_perm03_le_journal_enregistre_qui_a_accorde_quoi_et_quand`.

Y ajouter les deux tests que CLAUDE.md #13 impose et que `03-RESEARCH.md` section 4 avait
omis (`03-UI-SPEC.md` 0.1) :
`test_perm03_un_droit_est_stocke_par_gerant_magasin_et_permission` et
`test_perm03_un_droit_accorde_dans_un_magasin_ne_fuit_pas_vers_un_autre`.

`tests/test_magasin_acces.py`, PERM-04 et PERM-05, implementes aux plans 03-05 et 03-07 :
`test_perm04_un_gerant_ne_voit_que_les_magasins_accordes_en_liste_et_en_detail`,
`test_perm04_un_gerant_ne_peut_pas_ecrire_dans_un_magasin_non_accorde`,
`test_perm04_un_droit_obsolete_sur_un_magasin_inactif_n_accorde_rien`,
`test_perm04_lacces_du_proprietaire_est_lensemble_complet_et_non_un_filtre_saute`,
`test_perm05_un_agregat_est_calcule_sur_le_queryset_deja_filtre`,
`test_perm05_le_catalogue_declare_prix_achat_marge_et_ca_global`.

`tests/test_locale.py`, APP-01 et APP-03, implementes au plan 03-10 :
`test_app01_les_erreurs_de_lapi_sont_en_francais`,
`test_app03_un_montant_traverse_le_json_en_chaine_pas_en_flottant`,
`test_app03_le_formatage_serveur_respecte_la_fixture_partagee`,
`test_app03_la_date_courte_est_au_format_jj_mm_aaaa`,
`test_app03_aucun_montant_n_est_un_flottant_dans_une_reponse`.

Les tests qui ont besoin des deux locataires ou des deux magasins demandent les fixtures
`db_all` / `deux_magasins` dans leur signature, ce qui fait que
`pytest_collection_modifyitems` leur applique le bon marqueur `django_db` a la collecte.
  </action>
  <acceptance_criteria>
    - `grep -ho 'def test_[a-z0-9_]*' tests/test_comptes_auth.py tests/test_comptes_droits.py tests/test_magasin_acces.py tests/test_locale.py | sort -u | wc -l` retourne 24
    - chacun des 24 noms de `03-VALIDATION.md` (plus les deux ajouts par magasin) apparait exactement une fois
    - `uv run pytest --collect-only -q tests/test_comptes_auth.py tests/test_comptes_droits.py tests/test_magasin_acces.py tests/test_locale.py` sort en code 0, sans erreur de collecte
    - `uv run pytest -q -m pending tests/ --collect-only | tail -1` montre 24 tests collectes ou plus
    - `uv run pytest -x -q -m "not slow and not pending"` reste vert : aucun stub ne fuit dans la boucle rapide
    - `grep -c '^from plateforme.comptes' tests/test_comptes_droits.py` retourne 0, les imports sont locaux aux fonctions
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest --collect-only -q tests/test_comptes_auth.py tests/test_comptes_droits.py tests/test_magasin_acces.py tests/test_locale.py</automated>
  </verify>
  <done>Les 24 tests nommes de PERM-01 a PERM-05 et APP-01/APP-03 sont collectes, marques `pending`, et la boucle rapide reste verte.</done>
</task>

<task type="auto">
  <name>Task 3: Les douze tests nommes de PERM-06 et le contrat OpenAPI, en attente</name>
  <read_first>
    - `03-VALIDATION.md` : les douze tests PERM-06, menes par le test parametre
    - `03-RESEARCH.md` section 3, sous-section "What actually prevents drift: the test, not
      the abstraction" : la forme de la parametrisation
    - `03-RESEARCH.md` attaques A-03-03, A-03-04, A-03-06, A-03-07, A-03-08, A-03-09
    - `tests/test_migration_conventions.py` : l'idiome des tests au niveau du source deja
      present dans le depot, a imiter pour A-03-04
  </read_first>
  <files>tests/test_projection.py, tests/test_schema_contrat.py</files>
  <action>
`tests/test_projection.py`, PERM-06, implemente aux plans 03-06 et 03-07. Le test porteur
de la phase est le premier, et il doit etre ecrit **parametre des maintenant**, meme si le
registre est vide en phase 3 :

    @pytest.mark.pending
    @pytest.mark.parametrize("rendu", ["api", "export", "document"])
    @pytest.mark.parametrize(
        "cle,code",
        sorted(CHAMPS_PROTEGES.items()) or [("tests.RessourceFixture.valeur_protegee", None)],
    )
    def test_perm06_champ_protege_absent_de_lapi_de_lexport_et_du_document(rendu, cle, code):
        ...

`CHAMPS_PROTEGES` n'existe pas encore : importer le registre **dans le corps de la
fonction** et donner a la parametrisation une valeur de repli constante, de sorte que la
collecte fonctionne avant le plan 03-06. La docstring dit pourquoi : ajouter une ligne au
registre en phase 8 produira automatiquement trois assertions ici, et un rendu ajoute en
phase 9 qui ne lit pas le registre echoue des qu'il rejoint la liste `rendu`.

Les onze autres, chacun avec sa docstring citant sa route de contournement :
`test_perm06_tout_champ_de_modele_expose_est_classe` (A-03-08),
`test_perm06_le_schema_est_identique_pour_le_proprietaire_et_le_gerant` (A-03-09),
`test_perm06_un_champ_protege_est_optionnel_dans_le_schema` (P9),
`test_perm06_un_champ_protege_ne_peut_ni_trier_ni_filtrer` (A-03-07),
`test_perm06_un_champ_protege_ne_peut_pas_etre_ecrit_par_un_gerant` : affirmer que la
valeur est **inchangee**, pas qu'un 400 survient, parce que DRF ignore silencieusement une
cle inconnue ;
`test_perm06_aucune_vue_ne_renvoie_un_values_queryset` (A-03-04, au niveau du source),
`test_perm06_aucun_modele_metier_n_est_enregistre_dans_ladmin` : deja implemente au plan
03-01, ne pas le dupliquer, laisser un commentaire pointant vers
`tests/test_comptes_socle.py` ;
`test_perm06_le_renderer_html_est_absent_hors_developpement` (A-03-06),
`test_perm06_toute_tache_de_rendu_exige_acting_utilisateur_id` (A-03-03),
`test_perm06_lacces_absent_vaut_aucun_droit` (P2, le test de fail-closed),
`test_perm06_le_catalogue_servi_a_un_gerant_manager_est_deja_intersecte`
(`03-UI-SPEC.md` 7.7 et la recommandation du `03-UI-CHECK.md` : le code qu'un editeur ne
possede pas est **absent du JSON**, pas filtre dans le navigateur).

`tests/test_schema_contrat.py`, implemente au plan 03-10 :
`test_perm06_le_schema_committe_correspond_au_schema_genere`. Sa docstring dit ce qu'il
garde : le client TypeScript est genere depuis `web/src/api/schema.yml`, et un schema qui
change en silence est exactement le mode de defaillance de PERM-06 applique aux types.
  </action>
  <acceptance_criteria>
    - `grep -c 'def test_perm06_' tests/test_projection.py` retourne 11
    - `grep -c 'parametrize' tests/test_projection.py` retourne au moins 2, le test porteur est bien doublement parametre
    - `grep -c 'def test_perm06_le_schema_committe_correspond_au_schema_genere' tests/test_schema_contrat.py` retourne 1
    - `uv run pytest --collect-only -q tests/test_projection.py tests/test_schema_contrat.py` sort en code 0
    - `uv run pytest -x -q -m "not slow and not pending"` reste vert
    - `grep -c '^from plateforme.projection' tests/test_projection.py` retourne 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest --collect-only -q tests/test_projection.py tests/test_schema_contrat.py</automated>
  </verify>
  <done>Les douze tests nommes PERM-06 existent, le test porteur est deja parametre registre x rendus avec un repli qui rend la collecte possible, et la boucle rapide reste verte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Aucune nouvelle. Ce plan n'ajoute ni surface exposee, ni role, ni base, ni fonction
`SECURITY DEFINER`. Il ajoute la **preuve** que les surfaces des plans suivants seront
verifiees.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-07 | Repudiation | un test nomme absent de la suite fait passer une exigence pour faite alors que rien ne la prouve | mitigate | les 36 noms de `03-VALIDATION.md` existent comme fonctions collectees, comptes par un `grep` dans les criteres d'acceptation |
| T-03-08 | Tampering | un stub ecrit au niveau du module importe un symbole absent et casse la collecte, rendant toute la suite rouge pour une mauvaise raison | mitigate | convention de la phase 2 : imports locaux aux fonctions, marqueur `pending`, valeur de repli dans la parametrisation |
| T-03-09 | Info disclosure | un bug de portee magasin invisible parce que les fixtures ne fournissent qu'un magasin (P16) | mitigate | fixture `deux_magasins`, exigee par les tests PERM-04 |
| T-03-10 | Tampering | un `pending` oublie apres implementation : le test existe, ne tourne jamais, et personne ne s'en apercoit | mitigate | la porte de phase compte les `pending` restants ; le plan 03-10 les verifie a zero pour les exigences declarees faites |
</threat_model>

<verification>
- `uv run pytest --collect-only -q tests/` : code 0, aucune erreur de collecte
- `uv run pytest -x -q -m "not slow and not pending"` : vert, les stubs ne fuient pas dans la boucle rapide
- Le nombre de fonctions `test_perm0*` et `test_app0*` collectees est au moins egal au nombre de noms listes dans `03-VALIDATION.md`
- `grep -rn 'django_db_setup' conftest.py tests/` : aucun resultat
</verification>

<success_criteria>
- Chaque test nomme de `03-VALIDATION.md` existe, avec son nom exact, sa docstring citant
  son exigence, son marqueur `pending` et son import differe
- Les deux tests supplementaires imposes par CLAUDE.md #13 sur la granularite des droits
  existent
- Les factories de comptes ecrivent sur `default` et n'assignent jamais un mot de passe en clair
- Les fixtures fournissent deux magasins par locataire, comme elles fournissent deux locataires
- La boucle rapide reste verte
</success_criteria>

<output>
Apres execution, creer `.planning/phases/03-comptes-permissions-app-shell/03-02-SUMMARY.md`,
en y listant le nom de chaque stub cree et le plan qui l'implementera, de sorte que chaque
plan suivant sache exactement quel rouge il doit rendre vert.
</output>
